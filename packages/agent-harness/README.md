# @sciforge/agent-harness

Shadow-mode agent harness policy package for SciForge.

The package exports stable TypeScript contracts, a small profile registry, and a deterministic merge runtime that produces a `HarnessContract` and `HarnessTrace` without changing business behavior.
