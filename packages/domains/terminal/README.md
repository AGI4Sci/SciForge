# SciForge Terminal

Official terminal domain package for SciForge. It owns the terminal bottom-panel
UI, tab/session behavior, renderer command, toolbar placement, translations, and
renderer-side resource lifecycle. Process creation and I/O use the host's
owner-scoped controlled-process capability contract.
