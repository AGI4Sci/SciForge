{
  "name": "@sciforge/evidence-dag",
  "version": "0.2.0",
  "license": "MIT",
  "type": "module",
  "private": false,
  "description": "Evidence DAG compiler with Artifact Registry, structured SourceAnchor, assessment ledger, and immutable snapshots.",
  "sciforge": {
    "lifecycleLayer": "workers",
    "skillFacing": false,
    "sideEffects": "filesystem, network",
    "publicContract": true,
    "runtimeAdapter": false,
    "mcpServer": false,
    "language": "python",
    "packagingStrategy": "Python sidecar (stdlib + networkx, requires-python >=3.10). `npm run start` runs `pip install -e .` then `python -m evidence_dag.server` on :3897. The desktop app reaches it over HTTP only (SCIFORGE_EVIDENCE_DAG_SERVICE_URL) through the package desktop sidecar and embedded-view contract modules. LLM calls use only Model Router env (EDAG_MODEL_ROUTER_*); ship the src/ + ui/ + desktop/ + pyproject.toml."
  },
  "scripts": {
    "prestart": "python -m pip install -q -e .",
    "start": "python -m evidence_dag.server",
    "serve": "python -m evidence_dag.server",
    "test": "PYTHONPATH=src python -m unittest discover -s tests -v",
    "samples": "python samples/load.py"
  },
  "engines": {
    "python": ">=3.10"
  }
}
