# Computer-use worker runtime deps.
# Model HTTP client talks only to the local SciForge Model Router.
requests>=2.31.0
# Local desktop executor — cross-platform.
pyautogui>=0.9.54
mss>=9.0.0
pyperclip>=1.8.2
Pillow>=10.0.0
# Windows UI Automation COM binding (P3b); exact for generated-interface stability.
comtypes==1.4.16; sys_platform == "win32"
# MCP stdio transport (only needed for `--stdio`; HTTP sidecar works without it).
mcp==1.8.0
