# SciForge 项目协议

最后更新：2026-05-28

当前目标：继续开发 `packages/actions/computer-use`，把它推进为 Codex CLI 可发现、可运行、可验证的 Computer Use 拓展包。拓展包应能在每个线程自己的虚拟桌面里完成复杂可见工作，使用虚拟鼠标和虚拟键盘，不移动用户真实鼠标，不发送全局键盘事件，也不打断用户正常使用电脑。

旧任务历史已在 2026-05-27 同步到 GitHub；旧任务板可从 Git 历史恢复。当前 `PROJECT.md` 只记录下一阶段设计、任务和验收规则。

## 当前范围

- 主要工作范围是 `packages/actions/computer-use`。
- 只有当 package 边界无法独立推进时，才修改 `packages/observe/vision`、runtime 或 GUI。
- 复杂工作必须通过 package CLI/API/stdio/host-port 边界完成，不能用 GUI 私有状态、DOM、accessibility tree、Playwright DOM 或 shell 直写文件冒充 Computer Use 成功。
- “用户能看到”指每个 agent 线程有可观看或可回放的虚拟屏幕、鼠标轨迹、键盘输入、动作时间线、截图帧和 artifact refs；不是指 agent 操作用户当前真实桌面。
- 当前 package-owned target-bound host 是 deterministic test harness，只证明 package contract，不证明真实应用验收。
- L3 只有真实 Linux isolated desktop backend 在同一 virtual session 内完成跨应用工作流并产出当前证据后才能声明；contract/validator skeleton 或 target-bound cross-app fixture 不能声明用户级 L3 完成。

## 最新算法边界

Computer Use 采用三层循环：

```text
Evidence Loop:
  observe -> inspect/crop/VLM/OCR -> update evidence graph
  repeat until evidence is enough or blocked

Action Loop:
  plan action -> ground -> execute -> verify -> update evidence graph

Task Loop:
  evidence loop -> action loop -> evidence loop -> ... -> complete/blocked
```

边界规则：

- Evidence Loop 只允许不改变屏幕、窗口、viewport、focus、菜单、tab 或应用状态的观察型操作。
- 允许的 evidence 操作包括 recapture、wait until stable、crop、OCR、VLM describe、VLM compare、region detection、visual table/image inspection。
- 任何会改变可见状态的操作都必须进入 Action Loop，包括 scroll、hover、focus、open menu/dropdown、switch tab/window/panel、zoom view、page up/down、任意鼠标或键盘输入。
- 只要进入 Action Loop，就必须记录 before/after evidence、grounding、executor outcome、verification 和 action causality。
- 完成判断必须从当前 evidence ledger 查询得出，不能只依赖历史 trace、旧截图或 action history。

## 不可变规则
- 所有修改必须通用，不能为当前案例写硬编码补丁。
- Codex CLI 拓展的核心算法部分优先用 Python 写，方便人类查看、修改。
- 代码路径保持唯一真相源：发现冗余链路时删除或合并旧链路，避免长期并行实现。
- 旧逻辑和最终方案不一致时，删除旧逻辑，不做长期兼容。
- 单文件超过约 2000 行时必须拆分或登记拆分任务。
- 已完成的 TODO 需要打勾，并补充 evidence、日期和最终状态。

## 当前任务板

- [x] 更新 Computer Use 设计文档为“每线程虚拟桌面 + 主动视觉探索 + evidence ledger”版本。
  证据：`packages/actions/computer-use/vision_computer_use_agent_mvp.md` 已重写为 v0.3，并明确 Evidence Loop、Action Loop、Task Loop 的边界。

- [x] 实现 Evidence Ledger MVP。
  输出 `evidence-log.jsonl`、`evidence-snapshot.json`、`evidence-index.json` 和 compact planner brief。记录 observation、region、text、visual-object、vlm-claim、grounding、action、verification、artifact、uncertainty、completion-claim。索引可从 log 重建，log 是唯一真相源。
  证据（2026-05-27）：新增 `packages/actions/computer-use/sciforge_computer_use/evidence_ledger.py`，loop 在 `metadata.evidenceOutputDir` 下写 `evidence-log.jsonl`、`evidence-snapshot.json`、`evidence-index.json`、`planner-brief.json`；`packages/actions/computer-use/tests/test_evidence_ledger.py` 与 `tests/test_loop.py::test_loop_writes_evidence_ledger_refs_and_staleness` 覆盖 schema、索引、planner brief 和 loop refs。

- [x] 把现有 action loop 拆成 Evidence Loop / Action Loop / Task Loop。
  Evidence Loop 只能做只读观察；scroll、hover、focus、菜单、tab、zoom、键鼠输入全部转入 Action Loop。completion guard 必须读取 evidence ledger，而不是散落的临时状态。
  证据（2026-05-27）：`loop.py` 新增 `EvidenceLoop`、`ActionLoop`、`TaskLoop` 和 `CompletionGuard`，`run_computer_use_task()` 委托 TaskLoop；`tests/test_loop.py::test_loop_phase_boundaries_and_completion_guard_reject_stale_evidence` 覆盖 completion guard 拒绝 stale evidence。

- [x] 补 freshness / staleness 规则。
  click、type、press key、scroll、drag 后旧截图、旧 OCR、旧对象位置默认 stale；crop、OCR、VLM describe 不使屏幕 stale；保存文件后目录 listing 必须重新 observe。
  证据（2026-05-27）：`evidence_ledger.action_mutates_visible_state()` 定义 state-changing / read-only evidence action 边界，action record 用 `invalidates` 与 `evidence-index.json.staleBy` 标记旧可见证据 stale；`tests/test_evidence_ledger.py` 覆盖 click/scroll/focus/type_text stale 与 crop/OCR/VLM/wait observation-only 不 stale。

- [x] 让 visible viewer 不再产生不可解释的空白帧。
  viewer 应展示真实或可解释的 frame refs、虚拟鼠标、点击波纹、键盘输入、滚动、保存动作、action timeline、isolation flags 和 artifact refs。
  证据（2026-05-27）：`visible_viewer.py` 支持 `kind="placeholder"` explained frame；`tests/test_visible_run.py` 覆盖无 screenshot refs、缺失截图文件、placeholder validation，并继续验证 target-bound PPT/CSV/DOCX viewer。

- [x] 引入 `VirtualDesktopSession` / `SessionManager` 设计骨架。
  每个线程绑定独立 virtual display、virtual input queue、filesystem root、capture stream、replay bundle 和 input lease。没有 isolated input adapter 时 fail closed。
  证据（2026-05-27）：新增 `virtual_desktop_session.py` 与 `tests/test_virtual_desktop_session.py`，覆盖独立 session root、refs-first manifest、input lease 独占、缺少/非隔离 adapter fail closed 和 isolation flags。

- [x] 接入首个真实隔离桌面 backend。
  优先使用 Linux desktop + noVNC + LibreOffice/browser，完成 L1 smoke：点击输入框、输入文字、点击按钮、验证屏幕变化，并生成 viewer、trace、input logs 和 isolation flags。
  进展（2026-05-27）：新增 `isolated_desktop_backend_probe.py` 与 `tests/test_isolated_desktop_backend_probe.py`，可写 Linux noVNC + LibreOffice/browser readiness/blocked manifest；当前仅证明依赖 readiness/blocked，不启动 noVNC，不执行 L1 smoke，不声明该项完成。
  进展（2026-05-27）：新增 `isolated_desktop_l1_smoke_evidence.py` 与 `tests/test_isolated_desktop_l1_smoke_evidence.py`，定义真实 L1 completed evidence 闸门，拒绝 readiness-only manifest、package-owned target-bound channel、placeholder-only viewer、缺 pointer/keyboard input logs、缺当前截图、prior-round done 和 stale completion supports；当前仍未启动真实 backend，不声明该项完成。
  进展（2026-05-27）：`isolated_desktop_l1_smoke_probe.py` 增加 Linux-only `--execute` runner 尝试路径，按顺序启动 Xvfb/window manager/VNC/noVNC/browser，用 isolated `DISPLAY` 下的 `xdotool` 与截图工具执行点击、输入、点击按钮和截图，并把 result/trace/viewer/input logs/session refs/evidence ledger 交给 L1 evidence validator；当前环境尚未产生真实 Linux completed run，不声明该项完成。
  进展（2026-05-28）：L1 runner 增加进程组隔离、backend 进程早退检测、VNC/noVNC localhost 端口 readiness probe 和 fail-closed diagnostics；`tests/test_isolated_desktop_l1_smoke_probe.py` 覆盖进程早退、端口不 ready、截图失败和 mock Linux completed contract。后续严格 Docker L1 gate 已补齐真实 Linux completed run。
  进展（2026-05-28）：L1 evidence validator 在 existing refs 模式下读取初始/最终截图文件并比较 sha256，拒绝内容完全相同的截图，避免只靠 `screenChanged=true` 或不同路径伪造完成；focused tests 覆盖 identical screenshot content 被 blocked。后续严格 Docker L1 gate 已补齐真实 Linux completed run。
  进展（2026-05-28）：L1 runner 写 `backend-readiness-proof.json` 证明 VNC/noVNC localhost 端口曾 ready，并在清理后把 session/noVNC/capture/replay refs 标成 `closedAfterRun=true`，避免 completed manifest 留下已关闭 viewer 仍 running 的假状态。后续严格 Docker L1 gate 已补齐真实 Linux completed run。
  进展（2026-05-28）：L1 runner 默认分配本轮可用的 X display、VNC/noVNC localhost 端口并写通用 `isolated-runtime-resource-allocation.json`，避免多线程争用固定 `:99` / 5909 / 6089；无法分配时 fail closed，不启动 backend。`tests/test_isolated_desktop_l1_smoke_probe.py` 覆盖跳过占用资源和分配失败 blocked。后续严格 Docker L1 gate 已补齐真实 Linux completed run。
  进展（2026-05-28）：L1 evidence validator 要求 `backendReadinessProofRef`，并读取 proof payload 校验 backend kind、ready 状态、localhost-only VNC/noVNC endpoint 和 no shared/system input flags；`tests/test_isolated_desktop_l1_smoke_evidence.py` 覆盖缺失或无效 readiness proof 被 blocked。后续严格 Docker L1 gate 已补齐真实 Linux completed run。
  进展（2026-05-28）：L1 runner 写 `l1-executor-command-events.json`，让 pointer/keyboard input event 引用具体 isolated input executor command；L1 validator 要求 `executorCommandEventLogRef`，校验 input event 的 `commandEventLogRef`/`commandEventRef` 必须指向同一 command log，command event 的 action index、modality、returncode 与 isolated `DISPLAY` 必须匹配，并拒绝命令溯源缺失、失败命令、DISPLAY 不匹配或 command log 自带 shared/system input side-effect flags。`tests/test_isolated_desktop_l1_smoke_evidence.py` 与 `tests/test_isolated_desktop_l1_smoke_probe.py` 覆盖该合同。后续严格 Docker L1 gate 已补齐真实 Linux completed run。
  进展（2026-05-28）：L1 runner 为长生命周期 backend/browser 进程写 stdout/stderr log refs，避免未 drain PIPE 阻塞真实端口 readiness；Chromium-family 启动增加容器/root 友好 flags；L1 validator 要求每个 state-changing step 的 before/after 截图内容变化，避免只靠打开浏览器造成初末截图差异冒充点击/输入/按钮成功。`tests/test_isolated_desktop_l1_smoke_probe.py` 与 `tests/test_isolated_desktop_l1_smoke_evidence.py` 覆盖。后续严格 Docker L1 gate 已补齐真实 Linux completed run。
  进展（2026-05-28）：L1 smoke CLI 透出 `--display`、`--vnc-port`、`--novnc-port`、`--timeout-seconds` 和 `--resource-lock-root`，并在 `commandPlan.runnerOptions` 记录请求值，方便真实 Linux/CI 调试固定资源而不改代码。后续严格 Docker L1 gate 已补齐真实 Linux completed run。
  进展（2026-05-28）：L1 runner 在 noVNC TCP ready 后继续 GET localhost `/vnc.html`，把 HTTP status、bytesRead、sha256、HTML/noVNC marker 和 rawPayloadWritten=false 写入 `backend-readiness-proof.json`，L1 validator 要求该 HTTP viewer proof 存在且与 `noVncViewerRef.url` 匹配。后续严格 Docker L1 gate 已补齐真实 Linux completed run。
  进展（2026-05-28）：L1 runner 在启动 Xvfb 后用 isolated `DISPLAY` 下的 `xdotool getdisplaygeometry` 做 bounded polling，并把 queryable X display geometry 写入 `backend-readiness-proof.json`；validator 要求该 proof 存在、display 匹配、geometry 为正且匹配当前 L1 viewport。后续严格 Docker L1 gate 已补齐真实 Linux completed run。
  进展（2026-05-28）：L1 runner 在 browser 启动后等待 smoke page 设置的 ready title marker，用 isolated `DISPLAY` 下的 `xdotool search --onlyvisible --name` 与 `getwindowgeometry --shell` 证明真实 browser window/page 已可见，再进入输入动作；validator 要求 `backendReadinessProofRef.desktopWindow` 与 `page` proof 存在且 display/title/geometry/side-effect flags 有效。后续严格 Docker L1 gate 已补齐真实 Linux completed run。
  进展（2026-05-28）：L1 runner 将 pointer click 改为 window coordinate space 内的 `xdotool mousemove --window <windowId>`，并写 `targetWindowRef` 与 `windowBoundPointerProofRef` 证明每次 click 的 hit point 落在目标 bounds 内且绑定对应 executor command event；validator 要求 pointer input event 引用这些 refs，拒绝缺失 window-bound proof、坐标不在目标 bounds 内或裸全局 `mousemove x y click` 命令。后续严格 Docker L1 gate 已补齐真实 Linux completed run。
  进展（2026-05-28）：L1 completed evidence gate 现在要求 `processRef` 与 `resourceAllocationRef`；validator 读取 `sciforge.computer-use.backend-processes.v1` 和通用 `sciforge.computer-use.isolated-runtime-resource-allocation.v1` payload，校验 virtual-display/window-manager/vnc-server/novnc-proxy/browser 角色覆盖、存在的 stdout/stderr log refs、per-run isolated display/sessionId 与 VNC/noVNC localhost 端口、与 readiness proof 的 processRef/端口一致性、合法 allocation 状态以及显式 `false` 的 shared/system side-effect flags。L1 runner 在构建 completed evidence 前传入 allocation ref，并为 session、resource allocation 和 backend process records 写 display/sessionId/log refs。L1 backend `preflightRef` 仍允许 `diagnosticOnly=true` 作为依赖 readiness，但不能替代 completed evidence。后续严格 Docker L1 gate 已补齐真实 Linux completed run。
  进展（2026-05-28）：新增 `isolated_desktop_backend_bundle.py` / `isolated-desktop-backend-bundle-manifest.json`，把 package 内 `isolated_desktop_backend.Dockerfile` 纳入 manifest 和 package-data，记录 package-root Docker build context、apt dependency 清单、backend readiness run 命令、localhost-only noVNC L1 smoke run 命令、evidence output mount 和 claim limit；`action-provider.manifest.json` 暴露 `isolatedDesktopBackendBundle` 诊断入口，`package.json` 增加严格 Docker gate `smoke:cu-isolated-l1:docker` / `smoke:cu-isolated-l1:opt-in` 和 pytest skip-gated `smoke:cu-isolated-l1:pytest-opt-in`，`.github/workflows/cu-isolated-desktop-l1.yml` 提供手动 `workflow_dispatch` L1 smoke job，不接入默认 `verify`。该 bundle spec 不构建镜像、不启动 backend、不执行输入，不能替代真实 Linux completed L1 evidence；后续 Docker Desktop 可用，并由严格 Docker L1 gate 补齐 completed L1 evidence。
  进展（2026-05-28）：Docker Desktop daemon 已可启动并可连接；严格 Docker L1 gate 已尝试运行。默认 `python:3.12-slim-bookworm` base image 在 Docker Hub token 拉取阶段超时，`hubproxy.docker.internal:5555/library/python:3.12-slim-bookworm`、`hubproxy.docker.internal:5555/python:3.12-slim-bookworm` 和 `hubproxy.docker.internal:5555/docker.io/library/python:3.12-slim-bookworm` 均无法解析。为保持通用性，Dockerfile 改为 `ARG PYTHON_BASE_IMAGE=python:3.12-slim-bookworm` / `FROM ${PYTHON_BASE_IMAGE}`，`smoke:cu-isolated-l1:docker`、backend bundle manifest 与手动 workflow `base_image` 输入支持 `SCIFORGE_DOCKER_BASE_IMAGE` override。当时仍未构建出镜像，也未产生真实 Linux completed L1 evidence；后续严格 Docker L1 gate 已补齐该证据。
  进展（2026-05-28）：`mirror.gcr.io/library/python:3.12-slim-bookworm` 与 `public.ecr.aws/docker/library/python:3.12-slim-bookworm` 可拉取并解析到同一 digest；用 ECR base image 重跑严格 Docker L1 gate 已越过 base image 阶段，但在 Docker build 的 `apt-get install` 阶段因容器内连接 `deb.debian.org:80` 超时失败，尚未构建镜像或运行 L1。Dockerfile、bundle manifest、`package.json` gate 和手动 workflow 增加 `DEBIAN_APT_MIRROR`、`DEBIAN_SECURITY_APT_MIRROR`、`APT_ACQUIRE_RETRIES` / `SCIFORGE_DOCKER_DEBIAN_APT_MIRROR`、`SCIFORGE_DOCKER_DEBIAN_SECURITY_APT_MIRROR`、`SCIFORGE_DOCKER_APT_ACQUIRE_RETRIES`，只用于替换 apt 下载源与重试次数，不放宽 completed evidence gate。后续严格 Docker L1 gate 已补齐真实 Linux completed L1 evidence。
  进展（2026-05-28）：严格 Docker L1 gate 已用 `SCIFORGE_DOCKER_BASE_IMAGE=public.ecr.aws/docker/library/python:3.12-slim-bookworm`、`SCIFORGE_DOCKER_DEBIAN_APT_MIRROR=https://mirrors.tuna.tsinghua.edu.cn/debian`、`SCIFORGE_DOCKER_DEBIAN_SECURITY_APT_MIRROR=https://mirrors.tuna.tsinghua.edu.cn/debian-security`、`SCIFORGE_DOCKER_APT_ACQUIRE_RETRIES=5` 和 `SCIFORGE_CU_ISOLATED_L1_EVIDENCE_DIR=/tmp/sciforge-cu-isolated-l1` 产出首个真实 Linux/noVNC completed L1 run；`/tmp/sciforge-cu-isolated-l1/l1/isolated-desktop-l1-smoke-probe-manifest.json` 为 `status=completed`、`diagnosticOnly=false`、`runAttempted=true`、`inputExecuted=true`、`realWindowEvidence=true`、`userAcceptanceEligible=true`，`completionEvidenceRef=/evidence/l1/isolated-desktop-l1-smoke-evidence.json`。容器内独立复验 `validate_isolated_desktop_l1_smoke_evidence('/evidence/l1/isolated-desktop-l1-smoke-evidence.json')` 返回 `{'ok': True, 'errorCount': 0}`；最终截图显示真实按钮触发 `L1 screen changed: SciForge isolated desktop L1`。Runner 同步增加 run-owned session cleanup，避免复用 evidence dir 时 Chromium profile lock / 旧截图 / 旧日志污染下一轮 gate；Chromium-family command 加 `--test-type` 抑制 root/no-sandbox 测试横幅；button pointer target 收紧为真实按钮 window-local bounds/hit point。该项完成；后续 L3 same-session multi-app workflow 也已由严格 Docker gate 补齐 completed evidence。

- [x] 做可见多页 PPT 验收。
  在 package workflow 中完成多页 `.pptx`，记录通用 GUI action、保存动作、最终可见证据、文件证据、PPTX validator、slide count、无宏检查、viewer 和 input logs。PPT 不能成为特例算法。
  证据（2026-05-27）：`fixtures/target-bound-visible-three-page-pptx.json` 与 `tests/test_visible_run.py::test_visible_run_target_bound_three_page_pptx_uses_generic_actions` 验证三页 PPTX、generic action、viewer、input logs、slideCount=3 与 PPTX validation。该证据仍是 package-owned target-bound diagnostic，不是 L1/L2 真实隔离桌面验收。

- [x] 做可见 Word/DOCX 验收。
  使用 Word 或 Word-compatible isolated target 创建 `.docx`，包含标题、段落、项目符号和表格。若真实 Word 无法安全隔离，写 blocked manifest，并先用 Word-compatible target 验证通用 contract。
  证据（2026-05-27）：新增 DOCX renderer/validator、`fixtures/target-bound-docx-document.json`、`tests/test_target_bound_window_host_probe.py::test_target_bound_window_host_can_create_word_compatible_docx_artifact` 与 `tests/test_visible_run.py::test_visible_run_target_bound_docx_uses_same_generic_viewer`，验证标题、段落、项目符号、表格、DOCX validator、viewer 和 input logs。真实 Word/LibreOffice 隔离 backend 仍未接入，不声明真实应用验收。

- [x] 做跨应用文档工作流。
  在同一 virtual session 中读取可见资料，创建 Word 报告或 PPT 摘要，保存、预览、返回目录证据。禁止使用 DOM shortcut 或 shell 直写产物冒充 GUI 操作。
  进展（2026-05-27）：新增 `fixtures/target-bound-cross-app-document-workflow.json` 与 `tests/test_visible_run.py::test_visible_run_target_bound_cross_app_document_workflow_diagnostic`，验证 source reader -> Word-compatible writer -> file browser/preview 的 package-owned visible diagnostic、DOCX validator、目录 file-list refs、evidence ledger、viewer 和 input logs。真实 isolated session 多应用验收仍依赖首个 backend，未声明完成。
  进展（2026-05-27）：`isolated_desktop_l3_workflow_evidence.py` 定义 L3 isolated multi-app workflow completed evidence 的 contract / validator，要求真实 Linux isolated backend、同一 virtual session、source/writer/file-preview 三类 app、当前截图、input logs、source fact -> artifact causality、artifact/file-list/gui.present refs 和 `userAcceptanceEligible=true`。Validator 默认要求 existing refs，并读取 result/trace、viewer、session schemas、input logs、artifact validation、file-list/gui.present payload 和 current evidence ledger completion claim；`tests/test_isolated_desktop_l3_workflow_evidence.py` 覆盖 target-bound fixture/L1 smoke evidence schema/legacy L1-only schemas/shape-only refs 不能误提级。这只是验收闸门，不是 run 证据；当时尚无真实 Linux isolated L3 completed run，target-bound cross-app fixture 仍只是 package-owned diagnostic，不能声明用户级 L3 完成；后续严格 Docker L3 gate 已补齐真实 completed evidence。
  进展（2026-05-28）：新增 `isolated_desktop_l3_workflow_probe.py` 与 `tests/test_isolated_desktop_l3_workflow_probe.py`，写真实 L3 workflow fail-closed readiness/blocked manifest，复用 Linux noVNC + LibreOffice/browser backend readiness，并额外检查 isolated input、screenshot 和 file-preview 工具；默认 `readinessOnly=true`、`userAcceptanceEligible=false`，`--execute` 只有在真实 same-session 多应用 runner 产出 validator-accepted refs 后才能完成，测试覆盖 blocked probe manifest 不能通过 completed L3 validator。该阶段本身不声明跨应用文档工作流完成；后续严格 Docker L3 gate 已补齐真实 completed evidence。
  进展（2026-05-28）：L3 validator 在 existing refs 模式下读取 workflow screenshot refs 的 sha256，要求 source/writer/file-preview 视觉状态至少有三种不同截图内容，拒绝复制同一帧冒充多应用证据；`tests/test_isolated_desktop_l3_workflow_evidence.py::test_l3_workflow_validator_rejects_all_identical_screenshot_content` 覆盖相同截图内容被 blocked。后续严格 Docker L3 gate 已用不同 source/writer/file-preview 截图内容通过该 gate。
  进展（2026-05-28）：PPTX/DOCX artifact validators 增加通用 `textRuns` / `normalizedTextSha256`，L3 validator 读取 supported source fact payload 并要求每个 supported fact text 出现在 artifact validation 文本中，同时校验 artifact validation sha256 匹配 final artifact 文件；focused tests 覆盖无效 source fact、hash mismatch、缺 textRuns 和无关 artifact content 被 blocked。后续严格 Docker L3 gate 的 DOCX `textRuns` 包含 supported source facts 并通过该 gate。
  进展（2026-05-28）：L3 validator 要求 pointer/keyboard input event logs 覆盖 workflow 中对应 modality 的每个 action index，不再接受只出现一次 pointer/keyboard 的日志；focused tests 覆盖缺少 pointer step 或 type_text step 的 input event 被 blocked。后续严格 Docker L3 gate 已用 pointer/keyboard provenance 覆盖所有对应 action indexes。
  进展（2026-05-28）：L3 probe 在 ready+`--execute` 时写 `isolated-desktop-l3-runner-execution-boundary.json`，固化真实 same-session runner 的 GUI-only phase gates、artifact policy、input policy、session policy 和 required completed refs；只有 completed evidence validator 接受现有 refs 后才写 `completionEvidenceRef`，否则保持 blocked。
  进展（2026-05-28）：L3 completed evidence validator 增加共享 isolated desktop runtime/provenance proof parity，要求 `backendReadinessProofRef`、`executorCommandEventLogRef`、`processRef` 与 `resourceAllocationRef`，并读取 proof 校验 ready Linux/noVNC backend、queryable X display、localhost noVNC HTTP viewer、backend process stdout/stderr log refs、per-run display/port allocation 和每个 pointer/keyboard input event 的 isolated executor command provenance；L3 runner boundary 与 manifest 同步列出这些 required refs，并把 window-bound pointer proof 列为 completed L3 必需证据。后续严格 Docker L3 gate 已产出并通过这些 refs。
  进展（2026-05-28）：L3 completed evidence validator 现在强制要求 `targetWindowRef` 与 `windowBoundPointerProofRef`，并复用通用 window-bound pointer proof 校验：每个 pointer input event 必须使用 window coordinate space、引用目标 window/proof refs、命中目标 bounds、绑定对应 executor command event，且 executor args 必须包含 `xdotool mousemove --window <windowId>`；L3 `targetWindowRef` 必须使用通用 `sciforge.computer-use.isolated-target-window.v1` schema，不接受 L1 legacy target-window schema。后续严格 Docker L3 gate 已用 window-bound pointer proof 通过该 gate。
  进展（2026-05-28）：L3 `resourceAllocationRef` 改用通用 `sciforge.computer-use.isolated-runtime-resource-allocation.v1` schema，validator 保留 `sciforge.computer-use.l1-runtime-resource-allocation.v1` 作为 legacy alias，避免未来 L3 completed evidence 继续携带 L1-only 命名。后续严格 Docker L3 gate 已用通用 resource allocation schema 通过该 gate。
  进展（2026-05-28）：已新增 L3 partial same-session runtime runner / partial runtime refs 契约，用于在 same-session runner 尚未跑完整 source -> writer -> file-preview 工作流时记录部分 runtime 证据；该产物仍是 blocked diagnostic，不是 completed L3 evidence。partial refs 只允许出现在 `partialRunRef` / `partialRuntimeRefs`，completed-required top-level refs 在 blocked manifest 中必须显式为空；partial executor command log 只能是 `eventCount=0` 的 no-workflow-input 诊断日志，file-preview launcher 可以记录 `launcher-completed`，避免 `xdg-open`、`gio` 或文件管理器成功快速退出时误判 runtime 失败。partial refs 永远只能作为诊断/辅助输入，不能提升或复制为 completed namespace refs；completed L3 必须由真实 same-session runner 重新产出 top-level completed refs，并被 completed L3 validator 接受，且 `status=completed`、`l3Workflow.completed=true`、`diagnosticOnly=false`、`userAcceptanceEligible=true` 同时成立。Focused 回归 `PYTHONPATH=packages/actions/computer-use python -m pytest packages/actions/computer-use/tests/test_isolated_desktop_l3_workflow_probe.py packages/actions/computer-use/tests/test_isolated_desktop_l3_workflow_evidence.py packages/actions/computer-use/tests/test_plugin_probe.py packages/actions/computer-use/tests/test_loop.py::test_public_api_manifest_and_camel_case_aliases_are_stable -q`：73 passed in 1.00s；完整 package Python suite：355 passed, 1 skipped in 51.95s；CU-NEXT Node harness：32 passed；`python -m json.tool packages/actions/computer-use/action-provider.manifest.json >/dev/null && git diff --check` 通过。后续 Docker L1 已完成真实 Linux/noVNC L1，严格 Docker L3 gate 也已补齐真实 same-session completed L3 evidence。
  进展（2026-05-28）：新增 `isolated_desktop_l3_workflow_plan.py` 与 `tests/test_isolated_desktop_l3_workflow_plan.py`，定义 L3 runner 的 diagnostic-only action plan schema：source -> writer -> save -> preview -> validate，每步有 monotonic `actionIndex`、app role、expected modality 和 screenshot/observation required refs，并拒绝 `status=completed`、`userAcceptanceEligible`、shell/raw payload refs 或执行策略打开；`action-provider.manifest.json` 将该 builder/validator 记录在 L3 workflow requirements。它固化 runner 的动作索引/refs 合同，不执行 GUI、不生成 completed evidence；完成声明仍必须来自真实 runner + completed validator。
  进展（2026-05-28）：新增 `isolated_desktop_l3_workflow_result.py` 与 `tests/test_isolated_desktop_l3_workflow_result.py`，提供 fail-closed completed evidence assembler：先用 completed L3 validator 和 `require_existing_refs=true` 校验候选 payload，只有通过才写 `isolated-desktop-l3-workflow-evidence.json` / `completionEvidenceRef`；校验失败、shape-only validation 或候选 payload 携带 `partialRunRef` / `partialRuntimeRefs` 时只写 blocked assembly manifest，不写 completion evidence。该 assembler 是 runner 的 validate-before-write 组件，不执行 GUI；完成声明必须由真实 GUI runner 提供候选 refs 并通过 validator。Focused 回归 `PYTHONPATH=packages/actions/computer-use:packages/actions/computer-use/tests python -m pytest packages/actions/computer-use/tests/test_isolated_desktop_l3_workflow_result.py packages/actions/computer-use/tests/test_isolated_desktop_l3_workflow_plan.py packages/actions/computer-use/tests/test_isolated_desktop_backend_bundle.py packages/actions/computer-use/tests/test_plugin_probe.py packages/actions/computer-use/tests/test_loop.py::test_public_api_manifest_and_camel_case_aliases_are_stable -q`：29 passed in 0.24s；完整 package Python suite：375 passed, 1 skipped in 52.20s。
  进展（2026-05-28）：新增 `source_fact_evidence.py` 与 `tests/test_source_fact_evidence.py`，提供 refs-first source observation -> supported fact -> derived content payload helper；payload 声明 `compatibleSourceFactSchemaVersion=sciforge.computer-use.source-fact.v1` 以对齐 L3 supported fact validator，同时保持 `diagnosticOnly=true`、`rawPayloadWritten=false`、`inlineImageWritten=false`，拒绝 `userAcceptanceEligible`、`completionEvidenceRef`、shell refs 和 raw payload refs。该 helper 不读取 GUI/private state，不执行 shell，不声明 completed L3。Focused 回归 `PYTHONPATH=packages/actions/computer-use:packages/actions/computer-use/tests python -m pytest packages/actions/computer-use/tests/test_source_fact_evidence.py packages/actions/computer-use/tests/test_isolated_desktop_l3_workflow_result.py packages/actions/computer-use/tests/test_isolated_desktop_l3_workflow_plan.py packages/actions/computer-use/tests/test_plugin_probe.py packages/actions/computer-use/tests/test_loop.py::test_public_api_manifest_and_camel_case_aliases_are_stable -q`：31 passed in 0.25s；完整 package Python suite：376 passed, 1 skipped in 52.29s；CU-NEXT Node harness：32 passed；`npm run smoke:cu-isolated-l1:pytest-opt-in --silent`：1 skipped；JSON/YAML/diff checks 通过。
  进展（2026-05-28）：新增 `l3_artifact_bundle_evidence.py` 与 `tests/test_l3_artifact_bundle_evidence.py`，提供 refs-first artifact/file-list/gui.present diagnostic bundle helper；validator 要求 `finalArtifactRef`、`artifactValidationRef`、`fileListArtifactRef`、`fileListDataRef`、`guiPresentRef` 非空，校验嵌套 `artifactCausality` / `directoryEvidence` / `presentationEvidence` 与 top-level refs 一致，并拒绝 `completionEvidenceRef`、`userAcceptanceEligible`、`status=completed`、shell refs 和 raw payload refs。该 helper 不读取文件、不执行 GUI/shell、不写 completed evidence，不声明跨应用文档工作流完成。Focused 回归 `PYTHONPATH=packages/actions/computer-use:packages/actions/computer-use/tests python -m pytest packages/actions/computer-use/tests/test_l3_artifact_bundle_evidence.py packages/actions/computer-use/tests/test_isolated_desktop_backend_bundle.py packages/actions/computer-use/tests/test_plugin_probe.py packages/actions/computer-use/tests/test_loop.py::test_public_api_manifest_and_camel_case_aliases_are_stable -q`：26 passed in 0.25s；完整 package Python suite：383 passed, 1 skipped in 52.51s；CU-NEXT Node harness：32 passed；`npm run smoke:cu-isolated-l1:pytest-opt-in --silent`：1 skipped；JSON/YAML/diff checks 通过。
  进展（2026-05-28）：严格 Docker L3 gate 的 standalone baseline 曾用 `SCIFORGE_DOCKER_BASE_IMAGE=public.ecr.aws/docker/library/python:3.12-slim-bookworm`、`SCIFORGE_DOCKER_DEBIAN_APT_MIRROR=https://mirrors.tuna.tsinghua.edu.cn/debian`、`SCIFORGE_DOCKER_DEBIAN_SECURITY_APT_MIRROR=https://mirrors.tuna.tsinghua.edu.cn/debian-security`、`SCIFORGE_DOCKER_APT_ACQUIRE_RETRIES=5` 和 `/tmp/sciforge-cu-isolated-l3` 产出真实 Linux isolated same-session completed L3 run；`/tmp/sciforge-cu-isolated-l3/l3/isolated-desktop-l3-workflow-probe-manifest.json` 为 `status=completed`、`diagnosticOnly=false`、`runAttempted=true`、`inputExecuted=true`、`realWindowEvidence=true`、`userAcceptanceEligible=true`，`completionEvidenceRef=/evidence/l3/isolated-desktop-l3-workflow-evidence.json`。这是 L3 runner baseline 的旧绝对/容器内 ref 形态，只能证明 L3 runner 能完成；CU-NEXT completion evidence 不接受该旧 `/tmp` 外部证据，必须使用当前 round evidence bundle 内 canonical `isolated-desktop-l3-workflow-evidence.json`。容器内独立复验 `validate_isolated_desktop_l3_workflow_evidence('/evidence/l3/isolated-desktop-l3-workflow-evidence.json')` 返回 `{'ok': True, 'errorCount': 0}`；artifact validator 的 `textRuns` 为 `['Improved. Use two validation cohorts.']`，覆盖两个 supported source facts。Runner 在同一 isolated X display/session 中完成 source browser -> LibreOffice Writer GUI Save As DOCX -> Chromium directory preview，记录 window-bound pointer proof、keyboard command provenance、backend readiness proof、process/resource refs、file-list/gui.present refs、visible viewer 和 evidence ledger completion claim；partial refs 仍只能作为 blocked diagnostic，不能提升为 completed namespace refs。该项完成。
  验证（2026-05-28）：backend bundle + L3 plan focused tests `PYTHONPATH=packages/actions/computer-use python -m pytest packages/actions/computer-use/tests/test_isolated_desktop_l3_workflow_plan.py packages/actions/computer-use/tests/test_isolated_desktop_backend_bundle.py packages/actions/computer-use/tests/test_plugin_probe.py packages/actions/computer-use/tests/test_loop.py::test_public_api_manifest_and_camel_case_aliases_are_stable -q`：25 passed in 0.25s；完整 package Python suite：366 passed, 1 skipped in 52.28s；CU-NEXT Node harness：32 passed；`npm run smoke:cu-isolated-l1:pytest-opt-in --silent`：1 skipped；JSON/YAML/diff checks 通过。后续严格 Docker L1/L3 gates 已补齐真实 Linux/noVNC completed evidence。

- [x] 做高风险确认 demo。
  用虚拟输入填写安全字段，到发送/上传/删除类动作前返回 `needs-confirmation`，viewer 展示风险动作上下文但不执行。
  证据（2026-05-27）：新增 `fixtures/target-bound-visible-high-risk-confirmation.json` 与 `tests/test_visible_run.py::test_visible_run_target_bound_high_risk_confirmation_blocks_action_but_writes_viewer`，验证安全字段输入后高风险发送动作返回 `needs-confirmation`、无 final artifact、无 blocked action input event、approvalRequest refs、viewer 和 isolation flags。

- [x] 提升 artifact validator。
  保留 PPTX validator，增加 DOCX validator，并让 artifact evidence、file-list evidence、preview evidence、save causality 在 PPT、Word、CSV、表单、菜单、文件预览工作流中复用。
  证据（2026-05-27）：`artifact_renderers.py` 新增 `validate_docx_artifact` 和 Word-compatible DOCX renderer，`tests/test_artifact_renderers.py` 覆盖有效 DOCX、缺 `word/document.xml`、坏 XML、宏 payload、段落/表格/项目符号统计；target-bound evidence 继续复用 save causality。

- [x] 更新 README 与 CLI 示例。
  只记录已经能跑通并可验证的命令；不要把 roadmap 写成已支持能力。
  证据（2026-05-27）：`packages/actions/computer-use/README.md` 与 `vision_computer_use_agent_mvp.md` 补充 Evidence Ledger、viewer placeholder、session skeleton、package-local smoke/test 命令，并明确 package-owned diagnostics 与真实 app acceptance 边界。

## 近期 TODO

- [x] 为 evidence record 定义最小 schema 和 JSONL 写入器。
  证据（2026-05-27）：`evidence_ledger.py` 的 `EVIDENCE_RECORD_SCHEMA_VERSION`、`EvidenceLedger.append_record()` 与 `tests/test_evidence_ledger.py`。
- [x] 为 evidence index 实现 `current`、`byType`、`byRef`、`byActionIndex`、`byTag` 等基础索引。
  证据（2026-05-27）：`build_evidence_index()` 与 `tests/test_evidence_ledger.py`。
- [x] 为 planner brief 实现 deterministic query：最新观察、当前文本、当前对象、候选目标、阻塞 uncertainty、最近动作、artifact evidence、completion 缺口。
  证据（2026-05-27）：`build_planner_brief()`、`planner-brief.json` 写入和 `tests/test_evidence_ledger.py`。
- [x] 在现有 target-bound fixture 中记录 evidence log，先不接真实 VLM。
  证据（2026-05-27）：`virtual_desktop_probe.py` 给 child request 注入 `metadata.evidenceOutputDir`，result/trace/manifest 暴露 evidence refs；`tests/test_virtual_desktop_probe.py` focused 回归通过。
- [x] 给“状态改变操作不得进入 Evidence Loop”增加 focused regression。
  证据（2026-05-27）：`tests/test_evidence_ledger.py` 与 `tests/test_loop.py::test_targeted_wait_evidence_record_is_observation_only` 覆盖 state-changing action stale、observation-only wait 不 stale。
- [x] 给空白 viewer frame 增加显式失败或解释性 placeholder，避免用户误以为真实截图为空白。
  证据（2026-05-27）：`tests/test_visible_run.py` placeholder tests。
- [x] 运行 package-local Python 测试：`PYTHONPATH=packages/actions/computer-use python -m pytest packages/actions/computer-use/tests -q`。
  证据（2026-05-28）：316 passed in 36.82s。
- [x] policy/manifest TypeScript 改动后运行：`node --test --import tsx packages/actions/computer-use/provider-policy.test.ts packages/actions/computer-use/runtime-policy.test.ts`。
  证据（2026-05-28）：11 passed。
- [x] 每次提交前运行：`git diff --check`。
  证据（2026-05-28）：通过，无 whitespace error。
- [x] 给 Evidence/Action/Task loop 拆分增加 focused regression。
  证据（2026-05-27）：`tests/test_loop.py::test_loop_phase_boundaries_and_completion_guard_reject_stale_evidence`。
- [x] 给首个 isolated desktop backend 增加 readiness/blocked probe。
  证据（2026-05-27）：`isolated_desktop_backend_probe.py`、`tests/test_isolated_desktop_backend_probe.py`。
- [x] 给真实 isolated desktop L1 smoke 增加强 evidence validator，防止 readiness 或 package diagnostic 假通过。
  证据（2026-05-27）：`isolated_desktop_l1_smoke_evidence.py` 与 `tests/test_isolated_desktop_l1_smoke_evidence.py`。
- [x] 给真实 isolated desktop L1 smoke 增加 Linux-only runner 尝试路径和 CLI manifest 入口。
  证据（2026-05-27）：`isolated_desktop_l1_smoke_probe.py` 与 `tests/test_isolated_desktop_l1_smoke_probe.py` 覆盖缺依赖 blocked、ready 但截图失败 blocked、probe manifest 不能冒充 completed L1、以及 mock Linux runner 写 completed evidence 并通过 L1 validator；尚无真实 Linux completed run，PROJECT backend 任务仍未完成。
- [x] 给真实 isolated desktop L1 runner 增加进程/端口 readiness fail-closed 防护。
  证据（2026-05-28）：`isolated_desktop_l1_smoke_probe.py` 的 `SubprocessCommandRunner` 使用独立进程组，runner 在 Xvfb/window manager/VNC/noVNC/browser 启动后检测早退，并要求 VNC/noVNC localhost 端口 ready；`tests/test_isolated_desktop_l1_smoke_probe.py` 覆盖 backend process exited 与 port readiness timeout 均返回 blocked manifest。
- [x] 给真实 isolated desktop L1 evidence 增加截图内容变化 proof。
  证据（2026-05-28）：`validate_isolated_desktop_l1_smoke_evidence()` 在 `require_existing_refs=true` 时比较 `initialScreenshotRef` / `finalScreenshotRef` 文件 sha256；`tests/test_isolated_desktop_l1_smoke_evidence.py::test_l1_smoke_validator_rejects_identical_before_after_screenshot_content` 覆盖相同内容被拒绝。
- [x] 给真实 isolated desktop L1 runner 增加 backend readiness proof 与 closed refs 记录。
  证据（2026-05-28）：`isolated_desktop_l1_smoke_probe.py` completed mock run 写 `backend-readiness-proof.json`，清理后将 session/noVNC refs 标为 closed；`tests/test_isolated_desktop_l1_smoke_probe.py::test_l1_smoke_probe_completes_with_isolated_linux_runner_contract` 覆盖 readiness proof 与 closed refs。
- [x] 给真实 isolated desktop L1 runner 增加每轮 display/port allocation，避免并行线程端口冲突。
  证据（2026-05-28）：`isolated_desktop_l1_smoke_probe.py` 新增 isolated runtime resource lease，默认扫描可用 X display、VNC/noVNC localhost 端口并写通用 `isolated-runtime-resource-allocation.json`；`tests/test_isolated_desktop_l1_smoke_probe.py::test_l1_smoke_probe_allocates_unoccupied_display_and_ports_by_default` 与 `::test_l1_smoke_probe_blocks_when_runtime_resources_cannot_be_allocated` 覆盖资源跳过和 fail-closed。
- [x] 给真实 isolated desktop L1 evidence validator 增加 backend readiness proof 读取校验。
  证据（2026-05-28）：`validate_isolated_desktop_l1_smoke_evidence()` 要求 `backendReadinessProofRef`，并校验 `backend-readiness-proof.json` 的 ready/backend kind/localhost-only VNC 与 noVNC endpoint/side-effect flags；`tests/test_isolated_desktop_l1_smoke_evidence.py::test_l1_smoke_validator_requires_backend_readiness_proof_ref` 与 `::test_l1_smoke_validator_rejects_invalid_backend_readiness_proof_payload` 覆盖缺失和无效 proof。
- [x] 给真实 isolated desktop L1 input logs 增加 executor command provenance proof。
  证据（2026-05-28）：`isolated_desktop_l1_smoke_probe.py` 为三次 isolated input 写 `l1-executor-command-events.json`，input event 引用 `commandEventId` / `commandEventRef` / `commandEventLogRef`；`validate_isolated_desktop_l1_smoke_evidence()` 要求 `executorCommandEventLogRef`，并校验每个 pointer/keyboard event 对应的 command action index、modality、returncode、isolated `DISPLAY`、非 shell/system wrapper、匹配的 command event ref 和 no shared/system side-effect flags；`tests/test_isolated_desktop_l1_smoke_evidence.py::test_l1_smoke_validator_requires_input_event_command_provenance`、`::test_l1_smoke_validator_rejects_failed_input_command`、`::test_l1_smoke_validator_rejects_input_command_display_mismatch`、`::test_l1_smoke_validator_rejects_input_event_command_log_mismatch`、`::test_l1_smoke_validator_rejects_command_event_side_effect_flags` 与 `tests/test_isolated_desktop_l1_smoke_probe.py::test_l1_smoke_probe_completes_with_isolated_linux_runner_contract` 覆盖。
- [x] 给真实 isolated desktop L1 runner 增加真实 Linux/noVNC 启动硬化。
  证据（2026-05-28）：`isolated_desktop_l1_smoke_probe.py` 为长生命周期进程记录 `stdoutLogRef`/`stderrLogRef` 而不是保留未 drain pipe，Chromium-family browser command 抑制首启/后台 UI 并在 root/container 环境加 `--no-sandbox`；`validate_isolated_desktop_l1_smoke_evidence()` 要求每个 state-changing step 的 before/after 截图内容变化；`tests/test_isolated_desktop_l1_smoke_probe.py::test_l1_smoke_browser_command_adds_container_safe_chromium_flags`、`::test_l1_smoke_probe_completes_with_isolated_linux_runner_contract` 与 `tests/test_isolated_desktop_l1_smoke_evidence.py::test_l1_smoke_validator_rejects_unchanged_last_step_screenshot_content` 覆盖。后续严格 Docker L1 gate 已验证真实 Linux completed L1 run。
- [x] 给真实 isolated desktop L1 runner 增加 CLI runner 资源调参入口。
  证据（2026-05-28）：`isolated_desktop_l1_smoke_probe.py` 的 CLI 支持 `--display`、`--vnc-port`、`--novnc-port`、`--timeout-seconds` 和 `--resource-lock-root`，并把请求值写入 `commandPlan.runnerOptions`；`tests/test_isolated_desktop_l1_smoke_probe.py::test_l1_smoke_probe_cli_records_runner_options` 与 manifest/API assertion 覆盖。后续严格 Docker L1 gate 已验证真实 Linux completed L1 run。
- [x] 给真实 isolated desktop L1 runner 增加 noVNC HTTP viewer readiness proof。
  证据（2026-05-28）：`isolated_desktop_l1_smoke_probe.py` 在 noVNC TCP ready 后验证 localhost `/vnc.html` HTTP viewer，并只记录 `statusCode`、`bytesRead`、`sha256`、HTML/noVNC marker 与 `rawPayloadWritten=false`；`validate_isolated_desktop_l1_smoke_evidence()` 要求 `backendReadinessProofRef.novnc.httpViewer` 存在、localhost-only、2xx/3xx、非空 body hash、marker proof 且与 `noVncViewerRef.url` 匹配；`tests/test_isolated_desktop_l1_smoke_probe.py::test_l1_smoke_probe_blocks_when_novnc_http_viewer_is_not_ready`、`tests/test_isolated_desktop_l1_smoke_evidence.py::test_l1_smoke_validator_requires_novnc_http_viewer_proof` 与 `::test_l1_smoke_validator_rejects_invalid_novnc_http_viewer_proof` 覆盖。后续严格 Docker L1 gate 已验证真实 Linux completed L1 run。
- [x] 给真实 isolated desktop L1 runner 增加 X display readiness proof。
  证据（2026-05-28）：`isolated_desktop_l1_smoke_probe.py` 在 Xvfb 启动后用 `xdotool getdisplaygeometry` 验证 isolated `DISPLAY` 可查询，并把 `xDisplay` proof 写入 `backend-readiness-proof.json`；`validate_isolated_desktop_l1_smoke_evidence()` 要求 `xDisplay.ready=true`、display 匹配、width/height 为正并匹配 L1 viewport；`tests/test_isolated_desktop_l1_smoke_probe.py::test_l1_smoke_probe_blocks_when_x_display_is_not_queryable` 与 `tests/test_isolated_desktop_l1_smoke_evidence.py::test_l1_smoke_validator_requires_x_display_readiness_proof` 覆盖。后续严格 Docker L1 gate 已验证真实 Linux completed L1 run。
- [x] 给真实 isolated desktop L1 runner 增加 browser window/page readiness proof。
  证据（2026-05-28）：`isolated_desktop_l1_smoke_probe.py` 在 browser 启动后等待 smoke page 的 ready title marker，通过 isolated `xdotool search --onlyvisible --name` 和 `getwindowgeometry --shell` 写入 `backendReadinessProofRef.desktopWindow` 与 `page` proof；`validate_isolated_desktop_l1_smoke_evidence()` 要求 visible window、page ready marker、display/title/geometry 和 no shared/system input side-effect flags；`tests/test_isolated_desktop_l1_smoke_probe.py::test_l1_smoke_probe_blocks_when_browser_page_is_not_ready` 与 `tests/test_isolated_desktop_l1_smoke_evidence.py::test_l1_smoke_validator_requires_browser_window_and_page_proof` 覆盖。后续严格 Docker L1 gate 已验证真实 Linux completed L1 run。
- [x] 给真实 isolated desktop L1 runner 增加 window-bound pointer proof。
  证据（2026-05-28）：`isolated_desktop_l1_smoke_probe.py` 用 `xdotool mousemove --window <windowId>` 在 browser window-local coordinate space 内发送 pointer click，并写 `targetWindowRef`、`windowBoundPointerProofRef`、target bounds/hit point 与 command event binding；`validate_isolated_desktop_l1_smoke_evidence()` 要求 pointer input event 引用这些 refs 并拒绝裸全局 pointer command；`tests/test_isolated_desktop_l1_smoke_probe.py::test_l1_smoke_probe_completes_with_isolated_linux_runner_contract`、`tests/test_isolated_desktop_l1_smoke_evidence.py::test_l1_smoke_validator_requires_window_bound_pointer_proof` 与 `::test_l1_smoke_validator_rejects_bare_global_pointer_command` 覆盖。后续严格 Docker L1 gate 已验证真实 Linux completed L1 run。
- [x] 跑严格 Docker L1 gate，产出首个真实 Linux/noVNC completed L1 evidence。
  证据（2026-05-28）：`npm run smoke:cu-isolated-l1:docker` 使用 `public.ecr.aws/docker/library/python:3.12-slim-bookworm`、清华 Debian/security apt mirror、`SCIFORGE_DOCKER_APT_ACQUIRE_RETRIES=5` 和 `SCIFORGE_CU_ISOLATED_L1_EVIDENCE_DIR=/tmp/sciforge-cu-isolated-l1` 完成真实容器运行；`/tmp/sciforge-cu-isolated-l1/l1/isolated-desktop-l1-smoke-probe-manifest.json` 为 `status=completed`、`diagnosticOnly=false`、`runAttempted=true`、`inputExecuted=true`、`userAcceptanceEligible=true`，`completionEvidenceRef=/evidence/l1/isolated-desktop-l1-smoke-evidence.json`。容器内复验 validator 返回 `ok=True`、`errorCount=0`，最终截图显示按钮点击触发 `L1 screen changed: SciForge isolated desktop L1`。新增 `tests/test_isolated_desktop_l1_smoke_probe.py::test_l1_smoke_probe_resets_run_owned_session_state_before_execute` 覆盖复用 output dir 时清理 run-owned session/profile/viewer 状态；button pointer proof 断言真实按钮 bounds/hit point。
- [x] 拆分 `isolated_desktop_l1_smoke_probe.py` / `isolated_desktop_l1_smoke_evidence.py` 的 shared runtime 与 validator helpers。
  证据（2026-05-28）：新增 `isolated_desktop_runtime.py`，抽出资源租约、Subprocess runner、TCP/HTTP/X display/browser readiness、browser/VNC/noVNC command helpers；新增 `isolated_desktop_l1_smoke_evidence_helpers.py`，抽出 ref loading、screenshot hash/content checks、backend readiness proof、window-bound pointer proof 和 executor command provenance 校验。随后新增 `isolated_desktop_l1_smoke_probe_helpers.py`，继续把 L1 probe 的纯 helper 从主 entrypoint 中移出；当前 `isolated_desktop_l1_smoke_probe.py` 为 1899 行，新 helper 为 101 行，`isolated_desktop_l1_smoke_evidence.py` 为 891 行；保留 L1 probe CLI、manifest schema、public builder/validator import path、L3 fail-closed contract 不变。Focused 回归 `PYTHONPATH=packages/actions/computer-use python -m pytest packages/actions/computer-use/tests/test_isolated_desktop_l1_smoke_probe.py -q`：18 passed, 1 skipped；`python -m py_compile packages/actions/computer-use/sciforge_computer_use/isolated_desktop_l1_smoke_probe.py packages/actions/computer-use/sciforge_computer_use/isolated_desktop_l1_smoke_probe_helpers.py` 通过。后续严格 Docker L1 gate 已验证真实 Linux completed L1 run；该阶段本身不声明 completed L3；后续严格 Docker L3 gate 已补齐真实 completed evidence。
- [x] 拆分 `isolated_desktop_l3_workflow_probe.py` 的 L3 runner/probe helpers，满足单文件规模约束。
  证据（2026-05-28）：新增 `isolated_desktop_l3_workflow_probe_helpers.py`，抽出 L3 probe constants、JSON helpers、runtime/evidence payload helpers 和 file-preview helper；`isolated_desktop_l3_workflow_probe.py` 保留 CLI、manifest builder、completed/partial runner 主流程，并从 3211 行降到 1654 行，新 helper 为 1647 行。Focused 回归 `python -m py_compile packages/actions/computer-use/sciforge_computer_use/isolated_desktop_l3_workflow_probe.py packages/actions/computer-use/sciforge_computer_use/isolated_desktop_l3_workflow_probe_helpers.py` 与 `PYTHONPATH=packages/actions/computer-use python -m pytest packages/actions/computer-use/tests/test_isolated_desktop_l3_workflow_probe.py packages/actions/computer-use/tests/test_isolated_desktop_l3_workflow_probe_completed.py -q`：12 passed in 0.18s；行为边界不变，不声明新的 L3/CU-NEXT 完成。
- [x] 给高风险 visible confirmation demo 增加 fixture 和 viewer 测试。
  证据（2026-05-27）：`target-bound-visible-high-risk-confirmation.json` 与 focused visible_run test。
- [x] 给跨应用文档工作流增加 package-owned visible diagnostic。
  证据（2026-05-27）：`target-bound-cross-app-document-workflow.json` 与 focused visible_run test；该 target-bound diagnostic 本身不是真实验收。真实 isolated L3 baseline 后续已由严格 Docker gate 完成，CU-NEXT task completion 仍必须以各任务自己的当前 run evidence bundle 为准。
- [x] 给真实 isolated desktop L3 workflow 增强 completed evidence validator，防止 target-bound diagnostic、L1 smoke 或 shape-only refs 假通过。
  证据（2026-05-27）：`isolated_desktop_l3_workflow_evidence.py`、`tests/test_isolated_desktop_l3_workflow_evidence.py`、manifest/API/plugin discovery assertions；该阶段本身不声明 completed L3；后续严格 Docker L3 gate 已补齐真实 completed evidence。
- [x] 给真实 isolated desktop L3 workflow 增加 screenshot content diversity proof。
  证据（2026-05-28）：`validate_isolated_desktop_l3_workflow_evidence()` 在 `require_existing_refs=true` 时读取 workflow screenshot refs 并比较 sha256，要求至少三种不同截图内容；`tests/test_isolated_desktop_l3_workflow_evidence.py::test_l3_workflow_validator_rejects_all_identical_screenshot_content` 覆盖同一帧复制被 blocked。
- [x] 给真实 isolated desktop L3 workflow 增加 source fact -> artifact text causality proof。
  证据（2026-05-28）：`artifact_renderers.py` 的 PPTX/DOCX validators 输出 `textRuns`、`textRunCount`、`textCharCount` 和 `normalizedTextSha256`；`validate_isolated_desktop_l3_workflow_evidence()` 校验 supported source fact schema/text、artifact validation sha256 与 final artifact 文件一致，并要求 supported fact text 出现在 artifact validation 文本中；`tests/test_artifact_renderers.py` 与 `tests/test_isolated_desktop_l3_workflow_evidence.py` 覆盖文本抽取、hash mismatch、缺 textRuns 和无关 artifact content。
- [x] 给真实 isolated desktop L3 workflow 增加 input event action-index coverage proof。
  证据（2026-05-28）：`validate_isolated_desktop_l3_workflow_evidence()` 读取 result/trace steps 与 pointer/keyboard/input event logs，要求每个 required modality 的 workflow action index 都有对应 input event；`tests/test_isolated_desktop_l3_workflow_evidence.py::test_l3_workflow_validator_requires_pointer_events_for_pointer_steps` 与 `::test_l3_workflow_validator_requires_keyboard_events_for_type_and_save_steps` 覆盖缺失 action-index event 被 blocked。
- [x] 给真实 isolated desktop L3 workflow 增加 fail-closed readiness/blocked probe。
  证据（2026-05-28）：`isolated_desktop_l3_workflow_probe.py`、`tests/test_isolated_desktop_l3_workflow_probe.py`、manifest diagnostic probe 入口；缺 Linux/noVNC/LibreOffice/browser/file-preview readiness 时写 blocked manifest，依赖齐全和 `--execute` 也必须由真实 runner 产出 validator-accepted refs 才能写 completed evidence，blocked probe manifest 不能通过 completed L3 validator。后续严格 Docker L3 gate 已补齐真实 completed evidence。
- [x] 给真实 isolated desktop L3 workflow probe 增加 runner execution boundary ref。
  证据（2026-05-28）：`isolated_desktop_l3_workflow_probe.py` 在 ready+`--execute` 时写 `isolated-desktop-l3-runner-execution-boundary.json`，记录 launch-session、capture-source、write-artifact、save-through-gui、preview-directory、validate-and-present 六个 GUI-only phase gates，以及 forbid shell direct artifact write、same virtual session、isolated input action-index coverage 和 required completed refs；`tests/test_isolated_desktop_l3_workflow_probe.py::test_l3_workflow_probe_execute_is_fail_closed_even_when_dependencies_are_ready` 覆盖 boundary ref 与 blocked 状态。该阶段本身不声明 completed L3；后续严格 Docker L3 gate 已补齐真实 completed evidence。
- [x] 给真实 isolated desktop L3 workflow 增加 preflight/session payload 语义校验。
  证据（2026-05-28）：`validate_isolated_desktop_l3_workflow_evidence()` 在 existing refs 模式下加载 `preflightRef`，要求 Linux isolated backend ready payload、backend kind、observed backend components、noVNC web root，并拒绝 shape-only、diagnostic-only、readiness-only、user-acceptance-ineligible 或 fail-closed probe；同时校验 session/noVNC/capture/replay refs 的 session/display identity、localhost noVNC 和 capture stream workflow frame refs；`tests/test_isolated_desktop_l3_workflow_evidence.py::test_l3_workflow_validator_rejects_shape_only_preflight_ref`、`::test_l3_workflow_validator_rejects_readiness_only_l3_probe_as_preflight`、`::test_l3_workflow_validator_requires_session_runtime_refs_to_share_identity`、`::test_l3_workflow_validator_requires_novnc_viewer_localhost` 与 `::test_l3_workflow_validator_requires_capture_stream_workflow_screenshots` 覆盖。该阶段本身不声明 completed L3；后续严格 Docker L3 gate 已补齐真实 completed evidence。
- [x] 给真实 isolated desktop L3 workflow 增加共享 isolated desktop runtime/provenance proof parity。
  证据（2026-05-28）：`validate_isolated_desktop_l3_workflow_evidence()` 要求并读取 `backendReadinessProofRef`、`executorCommandEventLogRef`、`processRef` 和 `resourceAllocationRef`，校验 backend readiness proof、noVNC HTTP viewer、process stdout/stderr log refs、per-run resource allocation、每个 input event 的 commandEventId/ref/logRef、returncode、modality、actionIndex、isolated `DISPLAY` 与 no shared/system side-effect flags；`isolated_desktop_l3_workflow_probe.py` 与 manifest 的 L3 runner boundary 同步要求这些 refs，并把 window-bound pointer proof 列为 completed L3 必需证据。Focused 回归 `PYTHONPATH=packages/actions/computer-use python -m pytest packages/actions/computer-use/tests/test_isolated_desktop_l3_workflow_evidence.py packages/actions/computer-use/tests/test_isolated_desktop_l3_workflow_probe.py packages/actions/computer-use/tests/test_loop.py::test_public_api_manifest_and_camel_case_aliases_are_stable -q`：49 passed in 0.80s。该阶段本身不声明 completed L3；后续严格 Docker L3 gate 已补齐真实 completed evidence。
- [x] 给真实 isolated desktop L3 workflow 增加 window-bound pointer proof validator parity。
  证据（2026-05-28）：`validate_isolated_desktop_l3_workflow_evidence()` 要求并读取 `targetWindowRef` 与 `windowBoundPointerProofRef`，复用通用 window-bound pointer proof 校验，要求 pointer event 使用 window-local coordinate space、引用目标 window/proof refs、命中目标 bounds、绑定 executor command event，并拒绝未包含 `xdotool mousemove --window <windowId>` 的裸全局 pointer command；executor args 现在必须精确符合 `xdotool mousemove [--sync] --window <id> <x> <y> click 1` 顺序，不能把正确 x/y 藏在无关 token 里冒充命中；L3 还要求 `targetWindowRef` 使用通用 `sciforge.computer-use.isolated-target-window.v1` schema。`tests/test_isolated_desktop_l3_workflow_evidence.py::test_l3_workflow_validator_requires_window_bound_pointer_proof`、`::test_l3_workflow_validator_rejects_non_window_bound_pointer_command`、`::test_l3_workflow_validator_rejects_pointer_event_without_target_ref`、`::test_l3_workflow_validator_rejects_window_bound_proof_target_mismatch`、`::test_l3_workflow_validator_requires_generic_target_window_schema`、`::test_l3_workflow_validator_rejects_window_bound_display_mismatch`、`::test_l3_workflow_validator_rejects_window_bound_target_miss`、`::test_l3_workflow_validator_rejects_window_bound_command_window_mismatch`、`::test_l3_workflow_validator_rejects_window_bound_command_point_mismatch`、`::test_l3_workflow_validator_rejects_window_bound_command_spoofed_point_tokens` 与 `::test_l3_workflow_validator_rejects_window_bound_target_or_proof_side_effect_flags` 覆盖。该阶段本身不声明 completed L3；后续严格 Docker L3 gate 已补齐真实 completed evidence。
- [x] 给真实 isolated desktop L3 workflow 增加通用 runtime resource allocation schema。
  证据（2026-05-28）：`validate_isolated_desktop_l3_workflow_evidence()` 接受通用 `sciforge.computer-use.isolated-runtime-resource-allocation.v1`，并仅把 `sciforge.computer-use.l1-runtime-resource-allocation.v1` 作为 legacy alias；fixture 改为通用 schema，manifest 增加 `resourceAllocationSchemaRef` / `legacyResourceAllocationSchemaRef`。`tests/test_isolated_desktop_l3_workflow_evidence.py::test_l3_workflow_validator_accepts_legacy_resource_allocation_schema` 与 `::test_l3_workflow_validator_rejects_invalid_resource_allocation_schema` 覆盖。该阶段本身不声明 completed L3；后续严格 Docker L3 gate 已补齐真实 completed evidence。
- [x] 抽出真实 isolated desktop 共享合同常量，避免 L3 继续依赖 L1 命名真相源。
  证据（2026-05-28）：新增 `isolated_desktop_contracts.py`，集中定义 backend kind、target environment、capture source、input channel、backend readiness proof、executor command event log、通用 target-window schema 与通用 runtime resource allocation schema；backend/L1/L3 probe、L1/L3 validator、API/public shim 和 tests 改为从该模块读取共享常量，同时保留旧 import path 的兼容 re-export。L1 runner 现在写通用 `isolated-runtime-resource-allocation.json` 和 `sciforge.computer-use.isolated-target-window.v1` target-window payload；L1/L3 manifest 与 L3 runner boundary 同步暴露 `targetWindowSchemaRef`、`resourceAllocationSchemaRef` 和 legacy alias。Focused schema sync 回归 `PYTHONPATH=packages/actions/computer-use python -m pytest packages/actions/computer-use/tests/test_isolated_desktop_l3_workflow_probe.py packages/actions/computer-use/tests/test_loop.py::test_public_api_manifest_and_camel_case_aliases_are_stable -q`：7 passed in 0.13s；完整 package Python suite：316 passed in 35.86s；Node policy tests：11 passed；`python -m json.tool packages/actions/computer-use/action-provider.manifest.json >/dev/null && git diff --check` 通过。后续严格 Docker L1 gate 已验证真实 Linux completed L1 run；该阶段本身不声明 completed L3；后续严格 Docker L3 gate 已补齐真实 completed evidence。
- [x] 给真实 isolated desktop L3 runtime proof 增加 session-bound 校验，并补 L1/window-bound 小型 fail-closed 缺口。
  证据（2026-05-28）：`validate_isolated_desktop_l3_workflow_evidence()` 现在要求 `processRef` 与 `resourceAllocationRef` 携带匹配 `sessionManifestRef` / `virtualDisplayRef` 的 sessionId 和 display，新增负例覆盖 process top-level session mismatch、process record display mismatch 和 resource allocation session mismatch；L3 probe boundary/manifest 增加 `requiresSessionBoundRuntimeProofs`。`isolated_desktop_runtime.py` 新增中性 `IsolatedDesktopRunFailed`、`IsolatedDesktopResourceLease`、`DEFAULT_VIEWPORT`、`allocate_isolated_runtime_resources` 和 `SCIFORGE_COMPUTER_USE_RESOURCE_LOCK_DIR`，旧 L1 名称保留为兼容别名。L1 validator 现在要求 `targetEnvironmentKind` 精确匹配共享 Linux isolated desktop 常量，并且空/不完整 pointer command args 返回结构化 repair error 而不是抛异常。Focused 回归 `PYTHONPATH=packages/actions/computer-use python -m pytest packages/actions/computer-use/tests/test_isolated_desktop_l1_smoke_evidence.py packages/actions/computer-use/tests/test_isolated_desktop_l1_smoke_probe.py packages/actions/computer-use/tests/test_isolated_desktop_l3_workflow_evidence.py packages/actions/computer-use/tests/test_isolated_desktop_l3_workflow_probe.py packages/actions/computer-use/tests/test_loop.py::test_public_api_manifest_and_camel_case_aliases_are_stable -q`：110 passed in 7.90s；完整 package Python suite：323 passed in 36.64s；Node policy tests：11 passed；`python -m json.tool packages/actions/computer-use/action-provider.manifest.json >/dev/null && git diff --check` 通过。后续严格 Docker L1 gate 已验证真实 Linux completed L1 run；该阶段本身不声明 completed L3；后续严格 Docker L3 gate 已补齐真实 completed evidence。
- [x] 给真实 isolated desktop L1 preflightRef 增加 backend probe 语义校验。
  证据（2026-05-28）：新增 `isolated_desktop_preflight_evidence.py` 共享校验 backend preflight payload；L1 validator 在 existing refs 模式下读取 `preflightRef`，要求 backend probe schema、`status=ready`、Linux platform、匹配 backend kind、required observed components、`noVncWebRoot` 和全 ok 的 `preflightChecks`，并拒绝 shape-only、`readinessOnly=true`、`executeFailClosed=true`、非 Linux、错 backend、缺组件、缺 noVNC root 和 failed checks。L1 特意允许 backend probe 的 `diagnosticOnly=true` 作为依赖 readiness，但不允许它代替 completed evidence；L3 复用同一 helper 并继续拒绝 diagnostic-only/user-acceptance-ineligible preflight。Focused 回归 `PYTHONPATH=packages/actions/computer-use python -m pytest packages/actions/computer-use/tests/test_isolated_desktop_l1_smoke_evidence.py packages/actions/computer-use/tests/test_isolated_desktop_l3_workflow_evidence.py packages/actions/computer-use/tests/test_loop.py::test_public_api_manifest_and_camel_case_aliases_are_stable -q`：98 passed in 1.05s；完整 package Python suite：332 passed in 36.78s；Node policy tests：11 passed；`python -m json.tool packages/actions/computer-use/action-provider.manifest.json >/dev/null && git diff --check` 通过。后续严格 Docker L1 gate 已验证真实 Linux completed L1 run；该阶段本身不声明 completed L3；后续严格 Docker L3 gate 已补齐真实 completed evidence。
- [x] 给真实 isolated desktop L1 completed evidence 增加 runtime process/resource proof 语义校验。
  证据（2026-05-28）：新增 `isolated_desktop_runtime_proof_evidence.py` 共享 validator；`validate_isolated_desktop_l1_smoke_evidence()` 在 existing refs 模式下读取 `processRef` 和 `resourceAllocationRef`，要求 backend readiness proof 引用同一 processRef、process records 覆盖 virtual-display/window-manager/vnc-server/novnc-proxy/browser 角色、stdout/stderr log refs 存在、runtime proof display/session 与 L1 session/display 匹配、resource allocation schema/status/display/VNC/noVNC localhost ports 与 readiness proof 一致，并要求 runtime proof side-effect flags 显式为 `false`。`isolated_desktop_l1_smoke_probe.py` 在构建 completed evidence 前传入 allocation ref，并为 session、allocation 和 process payload 写 display/sessionId/log refs；manifest/docs 同步列出 required refs。Focused 回归 `PYTHONPATH=packages/actions/computer-use python -m pytest packages/actions/computer-use/tests/test_isolated_desktop_l1_smoke_evidence.py packages/actions/computer-use/tests/test_isolated_desktop_l1_smoke_probe.py packages/actions/computer-use/tests/test_isolated_desktop_l3_workflow_evidence.py packages/actions/computer-use/tests/test_loop.py::test_public_api_manifest_and_camel_case_aliases_are_stable -q`：126 passed in 8.03s；完整 package Python suite：345 passed in 37.30s；Node policy tests：11 passed；`python -m json.tool packages/actions/computer-use/action-provider.manifest.json >/dev/null && git diff --check` 通过。后续严格 Docker L1 gate 已验证真实 Linux completed L1 run。
- [x] 加固真实 isolated desktop L1 runner 的实际执行稳定性。
  证据（2026-05-28）：`isolated_desktop_l1_smoke_probe.py` 不再把 X display、VNC/noVNC、noVNC HTTP viewer 和 browser page readiness 等待硬截到 5 秒，而是使用 `--timeout-seconds` 的完整窗口；L1 smoke page 去掉 `autofocus`，并在 input focus 时改变可见 status 和 focus style，避免点击输入框前后截图完全相同导致真实 run 被 validator 拒绝；`tests/test_isolated_desktop_l1_smoke_probe.py` 新增 source guard、page focus 可见变化断言，以及 opt-in `SCIFORGE_RUN_REAL_L1_SMOKE=1` 真实 Linux/noVNC `--execute` 测试入口。Focused 回归 `PYTHONPATH=packages/actions/computer-use python -m pytest packages/actions/computer-use/tests/test_isolated_desktop_l1_smoke_probe.py -q`：17 passed, 1 skipped in 21.82s；当前 host 为 Darwin，Docker CLI 存在但 daemon 不可连接，因此 opt-in real Linux 测试未运行，不声明 backend 任务完成。
- [x] 给真实 isolated desktop L3 workflow 增加 GUI 保存与 GUI 目录预览 causality gate。
  证据（2026-05-28）：`validate_isolated_desktop_l3_workflow_evidence()` 现在要求 `artifactCausality` 声明 `savedThroughGui=true`、`shellDirectArtifactWrite=false`、匹配 top-level `finalArtifactRef` / `artifactValidationRef`，并要求 `savedByCommandEventRef` 匹配 `savedByActionIndex` 的 keyboard input event；`directoryEvidence` 要求 `previewedThroughGui=true`、`shellDirectoryListingOnly=false`、`previewedByInputModality=pointer`，并要求 `previewedByActionIndex` 有 pointer input event 支撑，防止 shell 直写 artifact 或 shell listing 冒充 GUI workflow。`isolated_desktop_l3_workflow_probe.py` 的 runner boundary、manifest、README 和 `vision_computer_use_agent_mvp.md` 同步公开这些要求；focused 回归 `PYTHONPATH=packages/actions/computer-use python -m pytest packages/actions/computer-use/tests/test_isolated_desktop_l1_smoke_probe.py packages/actions/computer-use/tests/test_isolated_desktop_l3_workflow_evidence.py packages/actions/computer-use/tests/test_isolated_desktop_l3_workflow_probe.py packages/actions/computer-use/tests/test_plugin_probe.py packages/actions/computer-use/tests/test_loop.py::test_public_api_manifest_and_camel_case_aliases_are_stable -q`：85 passed in 22.51s。该阶段本身不声明 completed L3；后续严格 Docker L3 gate 已补齐真实 completed evidence。
- [x] 给 plugin discovery probe 增加 manifest diagnostic command module 校验。
  证据（2026-05-28）：`plugin_probe.py` 递归收集 entrypoint probe 与 `hostPortsContract.diagnosticProbes` 的命令，解析并仅 import package-local `python -m sciforge_computer_use...` 模块，不执行命令；`tests/test_plugin_probe.py::test_plugin_probe_blocks_missing_diagnostic_probe_module`、`::test_plugin_probe_rejects_external_diagnostic_probe_module` 与 `::test_plugin_probe_blocks_unsafe_diagnostic_probe_command` 覆盖缺失、外部模块和 unsafe command。
- [x] 扩展 plugin discovery probe 的公开 API 与 diagnostic probe 覆盖。
  证据（2026-05-28）：`plugin_probe.py` 的 required API surface 现在覆盖 README/API 公开的 `validateRepairManifest`、visible viewer builders/validators、evidence ledger helper shims，并检查 `executorCommandEventLogSchema` / `EXECUTOR_COMMAND_EVENT_LOG_SCHEMA` 非 callable value alias 一致；`tests/test_plugin_probe.py` 明确覆盖 `isolatedDesktopBackend`、L1 readiness/execute、L3 readiness/execute diagnostic probe module import，`tests/test_loop.py` 也断言 backend probe manifest command。完整 package Python suite：350 passed, 1 skipped in 52.47s；Node policy tests：11 passed。

## 本地模型配置

- 本地 Computer Use 调试可以使用 ignored config，例如 `config.computer-use.local.json`。
- 这些文件可能包含 provider URLs、API keys、model names，绝不能提交或打印。
- Runtime Codex / Computer Use 服务环境必须通过 ignored config 或环境变量提供 `SCIFORGE_RUNTIME_API_KEY`，并配置 `SCIFORGE_PROXY_UPSTREAM_BASE_URL` 或 `SCIFORGE_RUNTIME_BASE_URL` 指向 provider upstream base URL；文档、日志和 repair action 只能引用变量名，不能打印 secret 值。
- 文本规划可以使用便宜的本地/项目文本模型。
- 可选 VLM evidence 必须通过代码中的 allowlist 和 sanitized diagnostics 证明模型存在，不能泄露 provider payload。

## 当前任务板：Computer Use 真实复杂任务矩阵

这些任务只用于下一轮 Computer Use 实测准备，并记录 Computer Use 真实复杂任务矩阵的实测状态；它们不能替代上面的真实 isolated desktop backend 或跨应用文档工作流验收。TUI Host -> `computer_use.runTask(request, hostPorts)` 必须产出 trace refs、before/after screenshots、focus crops、grounding diagnostics、final artifact refs、verifier verdict 和 `gui.present` refs；不得用 DOM、Playwright、accessibility tree、shell 直写或 app-private shortcut 冒充 GUI 操作。Package loop 会在 grounding 成功后、execute 前尝试只读 `hostPorts.crop(observation, region)` 生成 focus-crop evidence；缺 crop port、缺 grounding region 或 crop 失败不会改变屏幕，也不会把本轮 action 直接变成失败，但 CU-NEXT 真实任务验收仍必须提交实际 focus crop refs。

新增严格门槛（通用规则）：source approval sidecar 只能是 prior fail-closed approval sidecar 的 verbatim copy，except bundle-local refs，不得改写 payload 语义；copy 必须保留 original identity/hash/ref，包括原始 `approvalRequestId`、`riskActionHash`、canonical `approvalRef`、source sidecar ref 和内容 hash，不能用重新生成的 token 或摘要替代。`completionEvidenceRef` 不只证明 isolated L3 completed，还必须在 completion evidence payload 中显式绑定当前任务的 `finalArtifactRef`；当前轮 `gui.present` 必须展示同一 artifact ref，`finalArtifactRef` / `gui.present` / verifier / completion evidence 不能互相 fallback 或指向不同产物。`CU-NEXT-07` dense grounding marker 必须来自 dedicated `dense-grounding-rejections` sidecar（例如 `dense-grounding-rejections.json`）并记录被拒绝的 shortcut / fallback 候选；不得用 verifier ref、generic grounding diagnostics、trace/request ref 或普通 marker 字符串兜底。

### CU-NEXT-01 Literature to briefing deck

- [x] Run literature-to-briefing-deck through TUI Host -> `computer_use.runTask(request, hostPorts)` with real visual evidence - 2026-05-28 evidence: passed via `cu-next-01-real-final-artifact-gate-20260528T111428Z`, validate-run `status=ok`, marker `briefing-deck`.
- [x] Present trace refs、before/after screenshots、focus crops、grounding diagnostics、final artifact refs、verifier verdict 和 `gui.present` refs - 2026-05-28 evidence: round-04 `finalArtifactRef=report.md`, `gui.present` / verifier passed, same-round `completionEvidenceRef=isolated-desktop-l3-workflow-evidence.json`.

### CU-NEXT-02 Spreadsheet to chart report

- [x] Run spreadsheet-to-chart-report through TUI Host -> `computer_use.runTask(request, hostPorts)` with source -> spreadsheet -> report workflow refs - 2026-05-28 evidence: passed via `cu-next-02-real-final-artifact-gate-20260528T113244Z`, validate-run `status=ok`, marker `chart-report`.
- [x] Present trace refs、before/after screenshots、focus crops、grounding diagnostics、final artifact refs、verifier verdict 和 `gui.present` refs - 2026-05-28 evidence: round-04 `finalArtifactRef=report.md`, `gui.present` / verifier passed, same-round `completionEvidenceRef=isolated-desktop-l3-workflow-evidence.json`.

### CU-NEXT-03 Web research to mail draft

- [x] Run web-research-to-mail-draft through TUI Host -> `computer_use.runTask(request, hostPorts)` and stop at `needs-confirmation` before any external send - 2026-05-28 evidence: passed via `cu-next-03-real-approval-sidecar-gate-20260528T121738Z`, validate-run `status=ok`, marker `needs-confirmation`.
- [x] Present `gui.ask_user`, approval-request refs, `gui.present` refs、trace refs、before/after screenshots、focus crops、grounding diagnostics、executor lease、final artifact refs、final visible screenshot 和 verifier refs - 2026-05-28 evidence: round-04 `finalArtifactRef=report.md`, `approval-request.json` / `gui-ask-user.json` / `risk-audit.json`, same-round `completionEvidenceRef=isolated-desktop-l3-workflow-evidence.json`.

### CU-NEXT-04 File organization and index

- [x] Run file-organization-index through TUI Host -> `computer_use.runTask(request, hostPorts)` with file-list and final index artifact refs - 2026-05-28 evidence: passed via `cu-next-04-real-parallel-20260528T0647Z`, validate-run `status=ok`, marker `file-index`.
- [x] Present trace refs、before/after screenshots、focus crops、grounding diagnostics、final artifact refs、verifier verdict 和 `gui.present` refs - 2026-05-28 evidence: round-04 `finalArtifactRef=index.md`, `directory-listing.json`, `gui.present` / verifier passed, same-round `completionEvidenceRef=isolated-desktop-l3-workflow-evidence.json`.

### CU-NEXT-05 Failure recovery and multiturn repair

- [x] Run failure-recovery-multiturn-repair through TUI Host -> `computer_use.runTask(request, hostPorts)` with blocked manifest and repair hint refs - 2026-05-28 evidence: passed via `cu-next-05-real-20260528T133144Z`, validate-run `status=ok`, marker `repair-continuity`.
- [x] Resume the same trace/session and present trace refs、before/after screenshots、focus crops、grounding diagnostics、final artifact refs 和 continuation refs - 2026-05-28 evidence: round-05 `finalArtifactRef=virtual-document.md`, `blocked-manifest.json` / `repair-hint.json` / `continuation-request.json`, same-round `completionEvidenceRef=isolated-desktop-l3-workflow-evidence.json`.

### CU-NEXT-06 High-risk approval chain

- [x] Run high-risk-approval-chain through TUI Host -> `computer_use.runTask(request, hostPorts)` and return `needs-confirmation` plus `gui.ask_user` before risky submit - 2026-05-28 evidence: passed via `cu-next-06-real-confirmed-20260528T134624Z`, validate-run `status=ok`, marker `approval-ref`.
- [x] Resume only with explicit `approvalRef`, then present `gui.present` refs、trace refs、before/after screenshots、focus crops、grounding diagnostics、executor lease、final artifact refs、final visible screenshot、verifier refs 和 risk audit refs - 2026-05-28 evidence: round-04 `finalArtifactRef=report.md`, `approval-request.json` / `gui-ask-user.json` / `confirmed-request.json` / `risk-audit.json` plus `approval-source-request.json` / `approval-source-gui-ask-user.json` / `approval-source-risk-audit.json` / `approval-decision.json`, same-round `completionEvidenceRef=isolated-desktop-l3-workflow-evidence.json`.

### CU-NEXT-07 Visual grounding pressure test

- [x] Run visual-grounding-pressure-test through TUI Host -> `computer_use.runTask(request, hostPorts)` with dense grounding and rejected shortcut evidence - 2026-05-28 evidence: passed via `cu-next-07-real-final-artifact-gate-20260528T1028Z`, validate-run `status=ok`, marker `dense-grounding`.
- [x] Present trace refs、before/after screenshots、focus crops、grounding diagnostics、final artifact refs、verifier verdict 和 `gui.present` refs - 2026-05-28 evidence: passed with round-04 `report.md`, `gui.present`, verifier pass, and same-round `completionEvidenceRef=isolated-desktop-l3-workflow-evidence.json`.

### Shared CU-NEXT infrastructure progress

- 进展（2026-05-28）：新增 `tests/smoke/cu-next-evidence-classification.ts` 与 `tests/smoke/cu-next-evidence-classification.test.ts`，提供 CU-NEXT evidence classification/crosswalk pure function，将 evidence 标为 `fixture`、`package-local`、`target-bound-real`、`isolated-L1` 或 `isolated-L3`；只有 completed、real-window、`diagnosticOnly=false`、`userAcceptanceEligible=true` 的 `isolated-L1` 可满足 backend top-level，只有 completed、same-session、source -> writer -> file-preview causality、`diagnosticOnly=false`、`userAcceptanceEligible=true` 的 `isolated-L3` 可满足 L3 top-level，并拒绝 DOM/Playwright/accessibility/shared-system-input/shell-direct-artifact-write。扩展 CU-NEXT Node harness：38 passed；完整 package Python suite：383 passed, 1 skipped in 51.91s；JSON/YAML/diff checks 通过。该分类层只防止 CU-NEXT 准备板误提级，不运行真实任务，不勾选 CU-NEXT 或 L1/L3。
- 进展（2026-05-28）：CU-NEXT 分类器从 smoke test 文件提升为 `tools/computer-use-next/evidence-classification.ts`，`tests/smoke/cu-next-evidence-classification.ts` 仅 re-export，避免生产工具未来反向依赖测试目录；分类器优先识别 explicit fixture/package-local/target-bound 环境，防止 `acceptanceTier=l3-multi-app-workflow` 把 target-bound/package-local payload 误判成 isolated L3。`tools/cu-next-readiness-manifest.ts` 会调用该分类器，只有 completion-grade isolated-L3 evidence（completed、same-session、source -> writer -> file-preview causality、`diagnosticOnly=false`、`userAcceptanceEligible=true`、`realWindowEvidence=true`）才能提升 CU-NEXT readiness；acceptance-shaped target-bound/package-local evidence 仍会 blocked。该改动仍不运行真实 CU-NEXT 任务，不勾选 CU-NEXT 或 L1/L3。
- 进展（2026-05-28）：新增 `tools/computer-use-next/live-acceptance-validator.ts` 与 `tests/smoke/cu-next-live-acceptance-matrix.test.ts`，把 CU-NEXT-01..07 的真实 run evidence 从“refs 形状”升级到任务级语义 marker gate：briefing deck、chart report、needs-confirmation、file index、repair continuity、approvalRef、dense grounding；validator 要求 exact `taskId`、映射内 `scenarioId`、TUI Host/action provider/`gui.present` refs、screenshots、focus crops、grounding diagnostics、executor lease、final artifact、verifier refs、independent input session refs，并拒绝 fixture、dry-run、shared input、shell direct artifact、DOM/Playwright/accessibility。Task marker 必须来自 top-level `evidenceMarkers`，marker refs 必须是 evidence-bundle-local file refs 或允许的 `approval:` token；`CU-NEXT-03` 必须是 top-level `status=needs-confirmation` 才能表示正确停在外部发送前，`CU-NEXT-06` 必须有非空 canonical `approvalRef`。`tools/cu-user-acceptance-manifest.ts` 增加可选 `scenarioId` / `evidenceMarkers` / `completionEvidence`，`tools/cu-next-readiness-manifest.ts` 只把通过 live acceptance validator、completion-grade classification 且 marker refs 已落地的 manifest 当作 strong evidence。`smoke:real-task-matrix` 已串入 `smoke:cu-next-live-acceptance` 与 `smoke:cu-next-evidence-classification`；验证 `npm run smoke:real-task-matrix --silent` 通过。该 gate 不运行真实 CU-NEXT 任务，不勾选 CU-NEXT。
- 进展（2026-05-28）：`tools/cu-next-run.ts validate-run` 已接入同一 live acceptance validator：在 CU-LONG manifest / scenario-summary 通过且任务映射匹配后，继续要求 `cu-user-acceptance-manifest.json` 具备对应 CU-NEXT task marker、scenario binding、refs-first GUI/action-provider evidence 和禁止替代项；缺 marker、task/scenario 不匹配、`CU-NEXT-03` 非 `needs-confirmation` 或伪 live evidence 会返回 `repair-needed`。新增 `tests/smoke/cu-next-runner.test.ts` 覆盖缺 marker、marker ready、task/scenario mismatch 三类 validate-run 路径；验证 `npm run smoke:cu-next-runner --silent` 通过。该 runner gate 只收紧已产出 evidence 的校验，不运行真实 TUI Host 任务，不勾选 CU-NEXT。
- 进展（2026-05-28）：`src/runtime/computer-use/package-bridge.ts` 现在为每个 package-backed run 写 refs-first `tui-host-run-task-chain.json`，显式绑定 `computer-use-request.json`、`host-ports.json`、`tool-payload.json`、`vision-trace.json` 以及可选 `gui-present.json` / `gui-ask-user.json`，并在 trace 的 `packageBridge.tuiHostRunTaskChainRef` 暴露该链路 ref；`tools/cu-l3-independent-input-acceptance-harness.ts` 会把该 ref 纳入 TUI Host / `gui.present` evidence claims。该清单只记录 TUI Host -> package `run_task` -> GUI intent metadata 的证据链，不执行真实任务，不提升 target-bound/fixture evidence，也不勾选 CU-NEXT。
- 进展（2026-05-28）：新增通用 `tools/computer-use-next/acceptance-projection.ts`，让 CU-LONG passed round 的 package-bridge trace、independent-input-adapter、TUI Host sibling refs 和 verifier ref 自动投影为 CU-NEXT-01..07 的任务级 `evidenceMarkers`，并在 `tools/cu-l3-independent-input-acceptance-harness.ts` 中接入；projection 会写入结构化 `scenarioId`，`CU-NEXT-03` 通过显式 `explicitStatus={status: "needs-confirmation", scope: "high-risk-stop"}` 写入 `cu-user-acceptance-manifest.json`，不会把高风险停止误报成 `multi-app-workflow-passed`。`tools/cu-user-acceptance-manifest.ts` 的 `needs-confirmation` 状态只在完整 L3 pass evidence + 显式 high-risk stop 条件下出现，无显式状态时保留 pass/ready/blocked 原逻辑。Focused 验证：`npm run smoke:cu-next-live-acceptance --silent`、`npm run smoke:cu-next-runner --silent`、`npm run smoke:cu-next-user-acceptance-contract --silent`、`node --import tsx --test tests/smoke/cu-next-live-acceptance-matrix.test.ts tests/smoke/cu-l3-independent-input-acceptance-harness.test.ts tests/smoke/cu-next-runner.test.ts tests/smoke/cu-user-acceptance-manifest.test.ts`、`npm run typecheck --silent` 与 `git diff --check` 全部通过。该投影层只把当前真实 run refs 组织成通用验收 manifest，不执行真实 CU-NEXT 任务，不勾选 CU-NEXT。
- 进展（2026-05-28）：Package loop 现在会在 grounding 后、execute 前调用可选 `hostPorts.crop` 生成 `query="focus-crop"` observation，并把 crop refs 写入 observation `focusRefs` 与 grounding diagnostics；crop port 不存在、region 不足或 crop 失败只写 optional-evidence uncertainty，不把原动作直接变成失败。Runtime package bridge 同时写 dedicated evidence sidecars：always-on `directory-listing.json`，按需 `approval-request.json`、`risk-audit.json`、`confirmed-request.json`、`blocked-manifest.json`、`repair-hint.json`、`continuation-request.json`；Python/TS final-artifact filters 会把这些 sidecar 视作控制证据而非 final artifact。
- 进展（2026-05-28）：acceptance projection 对审批、修复连续性和目录类 marker 改为要求 dedicated sidecar refs：`approval-request.json` / `gui-ask-user.json`、`confirmed-request.json`、`risk-audit.json`、`blocked-manifest.json`、`repair-hint.json`、`continuation-request.json`、`directory-listing.json`。这些 marker 不再用 `vision-trace.json`、`computer-use-request.json`、verifier 或 `tui-host-run-task-chain.json` fallback 伪造；缺少专用 sidecar 会被 live acceptance validator 判为 invalid task marker。Focused 验证：Python focus-crop / trace promotion tests 3 passed；Node focused CU-NEXT/package-bridge/user-acceptance tests 53 passed；`npm run smoke:cu-next-live-acceptance --silent` 12 passed；`npm run smoke:cu-next-runner --silent` 13 passed；`npm run smoke:cu-next-user-acceptance-contract --silent` 5 passed；`npm run typecheck --silent` 与 `git diff --check` 通过。该改动仍只收紧验收投影，不运行真实 CU-NEXT 任务，不勾选 CU-NEXT。
- 进展（2026-05-28）：统一 high-risk stop 证据语义：package bridge 的 `approval-request.json` / `risk-audit.json` 与 CU-NEXT marker 都使用 `deniedExecuted=false` 表示被拒绝或等待确认的风险动作未执行；`tools/cu-next-run.ts` 的 projection success 判断也把 `CU-NEXT-03 status=needs-confirmation` 视为正确 high-risk stop，而不是 diagnostic failure。该改动只修正审批链证据语义和 run summary 判定，不运行真实 CU-NEXT 任务，不勾选 CU-NEXT。
- 进展（2026-05-28）：completion-grade gate 抽成 `tools/computer-use-next/completion-grade.ts`，`tools/cu-next-run.ts validate-run` 与 `tools/cu-next-readiness-manifest.ts` 共享同一 isolated-L3 判定；缺少 `completionEvidence`、缺少 `l3Workflow.completed=true` 或 target-bound/package-local completion shape 都会返回 `repair-needed` / blocked。`tools/cu-l3-independent-input-acceptance-harness.ts` 只透传显式 completion evidence，不按 passable L3 形状自动升格，并补 `--scenario-id` 与 nested `request.computerUseLong.scenarioId` 绑定。Focused 验证：`node --import tsx --test tests/smoke/cu-next-evidence-classification.test.ts tests/smoke/cu-next-readiness-manifest.test.ts tests/smoke/cu-next-runner.test.ts tests/smoke/cu-l3-independent-input-acceptance-harness.test.ts` 38 passed。该改动仍只收紧校验，不运行真实 CU-NEXT 任务，不勾选 CU-NEXT。
- 进展（2026-05-28）：completion-grade gate 进一步要求 `completionEvidenceRef` 指向 evidence bundle 内真实存在、validator-accepted 的 isolated desktop L3 workflow evidence JSON；inline `completionEvidence` 只能作为摘要，不能单独证明完成。`tools/cu-next-run.ts validate-run` 会把该 ref 纳入 required refs 并读取 JSON 传给 `tools/computer-use-next/completion-grade.ts`，`tools/cu-next-readiness-manifest.ts` 也用同一 bundle context 校验；shape-only / package-local / target-bound `completionEvidence` 即使其它 live marker 存在也会 blocked。Focused 验证：`node --import tsx --test tests/smoke/cu-next-readiness-manifest.test.ts tests/smoke/cu-next-runner.test.ts tests/smoke/cu-next-evidence-classification.test.ts`：33 passed；`npm run typecheck --silent` 通过。该改动仍只收紧校验，不运行真实 CU-NEXT 任务，不勾选 CU-NEXT。
- 进展（2026-05-28）：completion-grade gate 继续收紧到 completed-L3 required refs：`completionEvidenceRef` 的 JSON payload 必须带 `resultRef`、input/pointer/keyboard/executor command logs、backend readiness/process/resource allocation、target window/window-bound pointer proof、final artifact/artifact validation、file-list/gui.present、viewer/evidence ledger refs，以及非空 `screenshotRefs` / `traceRefs`；缺 `finalArtifactRef` 等 completed refs 会让 readiness / `validate-run` blocked。`tools/cu-l3-independent-input-acceptance-harness.ts` 现在只从当前 round evidence 目录里的 `isolated-desktop-l3-workflow-evidence.json` 或同目录 L3 assembly/probe manifest 的 bundle-local `completionEvidenceRef` 自动投影 completion evidence；absolute/outside refs（例如旧 `/tmp/sciforge-cu-isolated-l3/...`）会阻断 verifier，inline `completionEvidence` 仍不能替代 ref。Focused 验证：`node --import tsx --test tests/smoke/cu-l3-independent-input-acceptance-harness.test.ts tests/smoke/cu-next-readiness-manifest.test.ts tests/smoke/cu-next-runner.test.ts tests/smoke/cu-next-evidence-classification.test.ts`：45 passed。该改动只让同一 round 的真实 completed L3 evidence 可以被通用 bridge 发现，不复制旧 evidence，不运行真实 CU-NEXT 任务，不勾选 CU-NEXT。
- 进展（2026-05-28）：CU-NEXT-06 审批链改为 sidecar-content gate，而不是 token shape gate。新增 `tools/computer-use-next/approval-chain.ts`，`live-acceptance-validator`、`cu-next-run validate-run`、`cu-next-readiness-manifest` 和 independent-input acceptance harness 会读取 `approval-request.json` / `gui-ask-user.json` / `confirmed-request.json` / `risk-audit.json` 内容，要求同一 `approvalRequestId`、`riskActionHash`、canonical `approvalRef`、互相引用的 sidecar refs、`deniedExecuted=false` 和 `packageMayCallGuiDirectly=false`；session / trace / request 派生 approval token 被拒绝。Runtime package bridge 写出的 approval sidecars 同步带这些字段。Focused 验证：`node --import tsx --test tests/smoke/cu-next-live-acceptance-matrix.test.ts src/runtime/computer-use/package-bridge.test.ts` 24 passed；合并 focused Node suite 56 passed。该改动仍只收紧审批链验收，不运行真实 CU-NEXT-06，不勾选 CU-NEXT。
- 进展（2026-05-28）：审批链 gate 进一步拆成 `needs-confirmation` 与 `confirmed` 两个 phase。`CU-NEXT-03` 现在会读取并校验 `approval-request.json` / `gui-ask-user.json` / `risk-audit.json` 的内容，并显式拒绝 `confirmed-request` 混入；`CU-NEXT-06` 除四份当前 sidecar 外，还要求当前 evidence bundle 内的 `approval-source-request.json` / `approval-source-gui-ask-user.json` / `approval-source-risk-audit.json` / `approval-decision.json`，证明 confirmed retry 来源于 prior fail-closed request，而不是仅靠 `approval:` token 自造闭环。`gui.ask_user` sidecar 必须是 `port=gui.ask_user` 且携带 `payload.approvalRequest`，所有 approval sidecar 都必须带期望 schemaVersion、同一 `approvalRequestId` / `riskActionHash` / canonical `approvalRef` 和 `packageMayCallGuiDirectly=false`；`tools/cu-next-run.ts` 与 CU-LONG runner 新增通用 `--approval-ref` + `--approval-source-dir` confirmed retry 通路。`tools/computer-use-next/materialize-l3-completion-evidence.ts` 会把同轮 Docker L3 completed evidence 安全物化为当前 evidence bundle 顶层 canonical `isolated-desktop-l3-workflow-evidence.json`，拒绝绝对/外部/pseudo/parent/symlink refs。Focused 验证：`node --import tsx --test tests/smoke/cu-next-live-acceptance-matrix.test.ts tests/smoke/cu-next-runner.test.ts src/runtime/computer-use/package-bridge.test.ts` 53 passed。
- 进展（2026-05-28）：真实 CU-NEXT-01..07 全部通过 task-level live acceptance 与 completion-grade gate。通过的 manifest 分别为 `cu-next-01-real-final-artifact-gate-20260528T111428Z`、`cu-next-02-real-final-artifact-gate-20260528T113244Z`、`cu-next-03-real-approval-sidecar-gate-20260528T121738Z`、`cu-next-04-real-parallel-20260528T0647Z`、`cu-next-05-real-20260528T133144Z`、`cu-next-06-real-confirmed-20260528T134624Z`、`cu-next-07-real-final-artifact-gate-20260528T1028Z`；每个最终 acceptance bundle 都有 task-specific marker、bundle-local final artifact / `gui.present` / verifier refs，以及 same-round canonical `completionEvidenceRef=isolated-desktop-l3-workflow-evidence.json`。`CU-NEXT-03` 的 marker high-risk action 现在来自 `risk-audit.json`，`CU-NEXT-06` 的 marker 绑定 source approval sidecars 与 approval decision；对应 `npm run computer-use-next:validate-run -- --task CU-NEXT-0X --manifest ... --json` 均返回 `status=ok`。
- 进展（2026-05-28）：在补齐 PROJECT checklist acknowledgement 与审批链 provenance 后，最终 readiness gate 已通过：`npm run computer-use-next:readiness --silent -- --search-dir /tmp/sciforge-cu-next-real --kv-ground-smoke docs/test-artifacts/kv-ground-smoke/kv-ground-smoke.json --out /tmp/sciforge-cu-next-readiness-final-provenance-latest.json` 返回 `[ready] CU-NEXT readiness 7/7 passed; completionEligible=true`。该 readiness 仍同时依赖真实 task evidence gate、completion-grade isolated-L3 gate、Runtime Codex browser/KV-Ground global evidence 与 PROJECT checkbox acknowledgement，不能由任一单项替代。
- 进展（2026-05-28）：真实 CU-LONG/CU-NEXT preflight 的 KV-Ground `/health` gate 保持 fail-closed，但 diagnostics、matrix summary 和 repair plan 会脱敏 endpoint userinfo、query/hash、bearer/token/secret/password/apiKey；Python evidence ledger 的共同入口也做通用脱敏，防止 Authorization/header/body/raw payload/base64/data URL/token/secret/password/credential 进入 `evidence-log.jsonl`、index、snapshot 或 planner brief，并且 `_refs_from_value` 只索引安全 refs。Focused 验证：`node --import tsx --test tests/smoke/cu-next-runner.test.ts --test-name-pattern "KV-Ground|approval|validate-run|completion"` 18 passed；`PYTHONPATH=packages/actions/computer-use python -m pytest packages/actions/computer-use/tests/test_evidence_ledger.py packages/actions/computer-use/tests/test_loop.py -q` 45 passed。该改动仍只收紧依赖/日志/ledger 安全，不勾选 CU-NEXT。
- 进展（2026-05-28）：Runtime Codex browser release blocker 已用 Codex in-app Browser 重新采集当前证据并通过 strict gate：`docs/test-artifacts/runtime-codex-browser-acceptance/manifest.json` 为 `status=passed`，包含 default chat、single-turn、selected-ref follow-up 和 multi-turn 第二轮可见答案，以及 provider/model/profile/workspace/command id 与 folded audit evidence。新增通用 `tools/kv-ground-smoke.ts` / `npm run smoke:kv-ground -- --endpoint ... --image ... --text ...`，可用 inline image 调 live `/health` + `/predict/` 并写 `sciforge.kv-ground-smoke.v1`，不会把 endpoint userinfo/query/hash 或 inline image bytes 写入 manifest；当前独立 KV-Ground smoke 已生成 `docs/test-artifacts/kv-ground-smoke/kv-ground-smoke.json`，readiness 的 global evidence 现在显示 runtime browser 与 KV-Ground 均 passed。随后执行 `npm run computer-use-next:readiness --silent -- --search-dir /tmp/sciforge-cu-next-real --kv-ground-smoke docs/test-artifacts/kv-ground-smoke/kv-ground-smoke.json --out /tmp/sciforge-cu-next-readiness-after-browser-kv.json` 当时仍为 `blocked`、0/7 passed；剩余 blocker 是 CU-NEXT-01..06 缺 live L2/L3 user acceptance manifest，CU-NEXT-07 当时缺 completion-grade `completionEvidenceRef` / final artifact / verifier pass 等任务级证据。因此当时不勾选任何 CU-NEXT 项；后续 `cu-next-07-real-final-artifact-gate-20260528T1028Z` 已补齐 CU-NEXT-07 的 current-round final artifact、`gui.present` 和 canonical same-round completed-L3 evidence。
- 进展（2026-05-28）：CU-LONG runner 增加 acceptance action-count progress hint 与最终 aggregate validation：当 scenario acceptance 明确写出最低 generic / non-wait action count 时，`run-core.ts` 会把本轮建议动作 quota 作为 `acceptanceProgress` 传给 Runtime Codex planner，planner prompt 与 recent action summary 会显示当前动作计数；scenario rounds 写出后会重新运行 `validateComputerUseLongRun`，若总动作数、非 wait 动作数或 required refs 不足，就把 manifest / summary 从 passed 改为 `repair-needed`。Focused 验证：`node --import tsx --test src/runtime/codex/computer-use-text-planner.test.ts src/runtime/computer-use/host-adapter.test.ts` 14 passed；`npm run typecheck --silent` 通过。该改动不为 CU-NEXT-07 硬编码，也不勾选 CU-NEXT。
- 进展（2026-05-28）：已尝试真实 `CU-NEXT-07` run：`npm run computer-use-next:run-scenario -- --task CU-NEXT-07 --real --workspace-path /Applications/workspace/ailab/research/app/SciForge/workspace --out-root /tmp/sciforge-cu-next-real --run-id cu-next-07-real-focus-diagnostics-20260528` 产出 run dir `/tmp/sciforge-cu-next-real/CU-LONG-004/cu-next-07-real-focus-diagnostics-20260528`，四轮 CU-LONG round 曾通过，但独立 `npm run computer-use-next:validate-run -- --task CU-NEXT-07 --manifest /tmp/sciforge-cu-next-real/CU-LONG-004/cu-next-07-real-focus-diagnostics-20260528/manifest.json` 返回 `repair-needed`：真实 action count 为 15，低于 acceptance minimum 20；acceptance manifest 仍是 `blocked`，缺 final artifact / verifier pass / gui.present / validator-accepted completion evidence 等 required refs。因此不勾选 CU-NEXT-07，后续必须在真实 run 中补齐动作量和完成证据后再验收。
- 进展（2026-05-28）：CU-LONG / CU-NEXT repair-needed 诊断新增机器可读 `repairDiagnostics`，包含 `actionShortfall(s)`、`missingRefs`、`failingRoundDiagnosticsRefs`、`traceMetricsByRound`、`failureReasons` 和 `nextRepairFocus`；`validate-run --json`、`scenario-summary.json`、`cu-next-diagnostic-summary.json` 与 matrix summary 都会写出同一结构。对现有真实 CU-NEXT-07 run 执行 `npm run computer-use-next:validate-run --silent -- --task CU-NEXT-07 --manifest /tmp/sciforge-cu-next-real/CU-LONG-004/cu-next-07-real-focus-diagnostics-20260528/manifest.json --json` 返回 `repair-needed`，结构化显示 `actionCount observed=15 minimum=20 missing=5`，`missingRefs` 包含 `finalArtifactRef` / `completionEvidenceRef`，并给出下一步修复焦点。Focused 验证：`node --import tsx --test tests/smoke/cu-next-runner.test.ts --test-name-pattern "diagnostic summary|validate-run --json"` 20 passed；`npm run typecheck --silent` 通过。该改动只改善失败 run 的通用修复诊断，不把任何 CU-NEXT 项升级为完成。
- 进展（2026-05-28）：Runtime Codex planner 与 simulated remote-desktop adapter 增加通用 final-artifact hint：当当前 round/expectedTrace/requirements 要求 final artifact、evidence summary、action mapping 或字段/控件视觉证据总结时，planner 不应只靠截图/点击结束，而应先用安全 generic action 产出 visible typed/exported artifact；virtual remote session 现在把中文 `总结` / `视觉证据` / `动作映射` / `字段证据` / `控件证据` 识别为 artifact intent，并把对应 typed summary materialize 为 `report.md`，随后文件管理器可把它标为 `visible-and-saved`。Focused 验证：`node --import tsx --test src/runtime/codex/computer-use-text-planner.test.ts src/runtime/computer-use/independent-input-adapter.test.ts` 通过。该改动只提升通用 artifact refs 产出概率，不绕过 completion-grade / live acceptance gate，不勾选 CU-NEXT。
- 进展（2026-05-28）：`src/runtime/computer-use/package-bridge.ts` 的 evidence sidecars、approval/risk helpers 和 `tui-host-run-task-chain` 写入逻辑已拆到 `src/runtime/computer-use/package-bridge-evidence.ts`；主文件从 2098 行降到 1666 行，新 helper 为 480 行，行为与导出边界不变。Focused 验证：`node --import tsx --test src/runtime/computer-use/package-bridge.test.ts src/runtime/computer-use/host-adapter.test.ts`：17 passed。该拆分只满足单文件规模/所有权约束，不改变 CU-NEXT evidence gate，也不勾选 CU-NEXT。
- 进展（2026-05-28）：`tests/smoke/cu-next-runner.test.ts` 的 fixture builders、materializers 和 trace factories 已拆到 `tests/smoke/helpers/cu-next-runner-fixtures.ts`；主测试文件从 1964 行降到 1122 行，新 helper 为 864 行，测试语义不变。Focused 验证：`node --import tsx --test tests/smoke/cu-next-runner.test.ts`：20 passed；合并 CU-NEXT/harness focused suite：45 passed。该拆分只降低测试文件规模风险，不改变验收规则，不勾选 CU-NEXT。
- 进展（2026-05-28）：Runtime Codex direct-chat planner fallback 增加通用 transport hardening：OpenAI-compatible fallback 请求显式发送 `Accept-Encoding: identity`，并在 provider/proxy 错标 `content-encoding` 导致 Node fetch 解压失败（例如 `incorrect header check` / `terminated`）时，用同一 request body 和脱敏 diagnostics 重试 raw HTTP identity transport；raw transport 只在默认 fetch 解码失败后启用，不绕过 planner JSON schema、风险确认或 evidence gate。该 fallback 是 transport repair，不是 planner protocol fork；不得引入不同 JSON contract、不同 system prompt 或绕过现有 direct-chat schema 的 generic prompt 变体，避免 protocol drift。Focused 验证：`node --import tsx --test src/runtime/codex/computer-use-text-planner.test.ts`：9 passed；`npm run typecheck --silent` 通过。该改动修复真实 CU-NEXT-07 第四轮曾遇到的 planner timeout 后 fallback 解码 blocker，但不把任何 CU-NEXT evidence 提升为完成。
- 进展（2026-05-28）：CU-NEXT completion evidence gate 进一步收紧为 canonical same-round bundle-local ref：`completionEvidenceRef` 必须精确为当前 round evidence 目录内的 `isolated-desktop-l3-workflow-evidence.json`，并且该 JSON 仍需通过 completed isolated-L3 schema/status/refs gate；跨轮相对路径、reserved manifest 名、缺失 ref、`artifact:` 伪 ref、绝对/外部路径、symlink/realpath 逃逸或旧 `/tmp/sciforge-cu-isolated-l3` evidence 都会 blocked。前文所称自动投影只允许在当前 round evidence bundle 内发现这个 canonical filename/ref；不得从旧 L3 run、外部路径、inline summary 或 passable shape 推导完成。`tools/cu-l3-independent-input-acceptance-harness.ts` 先检查 bundle-local，再检查 canonical filename，再拒绝 symlink 并确认 realpath 仍在当前 round bundle，最后读取 validator-accepted payload；`tools/computer-use-next/evidence-classification.ts` / `completion-grade.ts` 共用同一 canonical ref 判定。Focused 验证：`node --import tsx --test tests/smoke/cu-l3-independent-input-acceptance-harness.test.ts tests/smoke/cu-next-evidence-classification.test.ts src/runtime/codex/computer-use-text-planner.test.ts`：33 passed；`npm run typecheck --silent` 与 `git diff --check` 通过；`npm run computer-use-next:readiness --silent -- --search-dir /tmp/sciforge-cu-next-real --kv-ground-smoke docs/test-artifacts/kv-ground-smoke/kv-ground-smoke.json --out /tmp/sciforge-cu-next-readiness-after-canonical-and-planner-fallback.json` 仍为 `blocked`、0/7 passed，说明 gate 没有误提级。
- 进展（2026-05-28）：CU-NEXT completion evidence gate 继续收紧 nested refs：canonical `isolated-desktop-l3-workflow-evidence.json` 不只看 shape，还要求 `evidenceKind=isolated-L3`、`targetEnvironmentKind=linux-isolated-desktop-session`，并要求 completed-L3 required refs、`screenshotRefs` 和 `traceRefs` 都能作为当前 evidence bundle 内 regular file 解析，且 symlink/realpath 不逃逸；`acceptanceTier=l3-multi-app-workflow` 不能单独推断 isolated-L3，`generated-file-only` / `api-created-artifact` shortcut 也会被 classifier 拒绝。Runtime planner 同步记录 fenced/embedded JSON 与 tool-call-like output 的 protocol drift 诊断，并把 tool-call wrapper 视为 retryable contract violation；direct-chat fallback 错误体复用 Runtime Codex audit scrubber 脱敏 bearer、token URL、api_key、HTML 和 `sk-` secrets。Focused 验证：`node --import tsx --test tests/smoke/cu-l3-independent-input-acceptance-harness.test.ts tests/smoke/cu-next-readiness-manifest.test.ts tests/smoke/cu-next-runner.test.ts tests/smoke/cu-next-evidence-classification.test.ts src/runtime/codex/computer-use-text-planner.test.ts src/runtime/vision-sense/computer-use-plan.test.ts`：86 passed。该改动仍只收紧验收与 planner 协议诊断，不运行真实 CU-NEXT 任务，不勾选 CU-NEXT。
- 进展（2026-05-28）：此前 `CU-NEXT-07` real rerun `/tmp/sciforge-cu-next-real/CU-LONG-004/cu-next-07-real-low-risk-quota-20260528T0825Z` 已 terminal `repair-needed`。这次没有再出现 provider/proxy 解压 blocker，也没有 high-risk Export/Share/Save blocker；四轮 trace validation 均 passed，actionCount 分别为 5/5/4/5，累计 19 个 non-wait GUI actions、62 个 screenshot refs、4 个 action ledger 和 4 个 failure diagnostics。`validate-run --json` 返回 `status=repair-needed`，核心 issue 是 `real run action count 19 is below acceptance minimum 20`（missing=1）；scenario 未 passed，因此 acceptance projection skipped、无 completed user acceptance / canonical `completionEvidenceRef` / final verifier pass。因此不勾选 CU-NEXT-07。后续 KV-Ground 恢复后的 rerun 已消除该 action shortfall，但仍需当前 evidence bundle 内的 final artifact、`gui.present`/verifier pass 和 completed-L3 evidence。
- 进展（2026-05-28）：CU-LONG acceptance progress 改为剩余配额驱动：每轮 planner contract 会携带已观察到的 scenario action / non-wait action 数、剩余数量和剩余轮数，`suggestedCurrentRoundActionTarget` / `suggestedCurrentRoundNonWaitActionTarget` 用 `ceil(remaining / remainingRounds)` 覆盖前序 shortfall；Runtime planner prompt 明确当前轮目标可能包含前轮缺口，且 planner `done=true` 在当前轮配额未达、仍有可执行 step 时会被 `quota-unmet` 拒绝。`src/runtime/computer-use/package-bridge.ts` 的 action-ledger completion 现在也 quota-aware：即使 Python policy 认为 validation/settings workflow complete，只要当前轮 action quota 未达且仍有 step budget，就不会清空队列或结束 round，而是继续安全 generic action 或结构化 failure，避免绕过 planner done gate。`tools/cu-next-run.ts validate-run` 同步补齐 completion evidence 文件本身的 regular-file / no-symlink / realpath-in-bundle gate，`completionEvidenceRef` 和 nested refs 复用同一 current bundle regular ref 检查。Focused 验证：`node --import tsx --test tests/smoke/cu-l3-independent-input-acceptance-harness.test.ts tests/smoke/cu-next-readiness-manifest.test.ts tests/smoke/cu-next-runner.test.ts tests/smoke/cu-next-evidence-classification.test.ts src/runtime/codex/computer-use-text-planner.test.ts src/runtime/vision-sense/computer-use-plan.test.ts src/runtime/computer-use/package-bridge.test.ts`：100 passed；`npm run typecheck --silent` 与 `git diff --check` 通过。该改动只修复通用 continuation/quota 和 completion ref 文件闸门，不放宽 completion gate，也不勾选 CU-NEXT。
- 进展（2026-05-28）：KV-Ground 恢复后继续修复 `CU-NEXT-07`：同一 run `/tmp/sciforge-cu-next-real/CU-LONG-004/cu-next-07-real-kv-recovered-20260528T0859Z` 的第 3 轮 quota repair 达到 10 个 non-wait actions，第 4 轮 summary repair 保留 1 个当前轮非 wait GUI evidence，四轮 trace 均 passed，累计 21 个 non-wait GUI actions、68 个 screenshot refs、4 个 action ledger 和 4 个 failure diagnostics。`validate-run --json` 当时仍为 `repair-needed`，不再有 action shortfall；blocker 收敛为 round-04 acceptance manifest `status=blocked`，缺 `finalArtifactRef`、`gui-present-record` evidence claim / `verifierVerdict.status=passed` 和 canonical `completionEvidenceRef`。后续 `cu-next-07-real-final-artifact-gate-20260528T1028Z` 已用 current-round final artifact/report gate 和同轮 completed-L3 evidence 修复该缺口。
- 进展（2026-05-28）：CU-LONG action quota 分摊改为只计算剩余 action-eligible rounds；refs-only summary/report round 不再吸收 scenario action shortfall，但每个 round-scoped CU-LONG/CU-NEXT 仍必须先产生至少一个当前轮非 wait GUI action evidence，planner `done=true` 才能成立。`src/runtime/vision-sense/computer-use-plan.ts` 增加 `current-round-action-missing` retryable contract issue，防止 summary round 只凭旧截图/旧 trace 完成；`tools/computer-use-long-task-pool/run-core.ts` 记录 `currentRoundActionQuotaEligible`、`remainingActionQuotaRounds` 和 eligibility reason。Focused 验证：CU-LONG quota progress、current-round action gate、package bridge quota-aware completion 和 CU-NEXT harness/readiness/runner suite 均通过；合并 Node focused suite 92 passed，相关 Python L3/L1/target-bound suite 160 passed，`npm run typecheck --silent` 与 `git diff --check` 通过。
- 进展（2026-05-28）：真实 isolated desktop L3 completed evidence writer 改为 bundle-local 输出：`isolated_desktop_l3_workflow_result.py` 在 completed validator 接受后，把 output bundle 内 JSON/JSONL 的绝对 refs 规范化为相对 refs，`completionEvidenceRef` 返回 canonical `isolated-desktop-l3-workflow-evidence.json`；`isolated_desktop_l3_workflow_evidence.py` 能按 evidence 文件所在目录解析 bundle-local refs，并继续拒绝 URL、pseudo refs、parent traversal、symlink 和 realpath escape；`visible_viewer.py` 也按 viewer manifest 所在目录解析相对 frame/input refs。严格 Docker L3 gate 重新运行到 `/tmp/sciforge-cu-isolated-l3-localized-20260528b/l3`，manifest 为 `status=completed`、`completionEvidenceRef=isolated-desktop-l3-workflow-evidence.json`；Python `validate_isolated_desktop_l3_workflow_evidence(...)` 返回 `ok=True`、`errorCount=0`，TS `cuNextCompletedL3CompletionEvidenceIssues(...)` 返回 0 issues，ref scan `badRefCount=0`。该证据证明新 L3 输出格式与 CU-NEXT completion-grade gate 对齐，但仍不能自动补齐任何 CU-NEXT task 的当前 round final artifact。
- 进展（2026-05-28）：Runtime planner/package bridge 增加通用 final artifact/report intent gate：当任务、requirements 或 expected trace 明确要求 artifact/report/summary/index/field-control/visual-evidence refs 时，planner `done=true` 必须有当前轮可见 final artifact/report ref；普通保存文档或诊断截图不会被误判成 final artifact 任务。`src/runtime/vision-sense/computer-use-plan.ts` 会用 `visible-artifact-missing` 拒绝 premature done，compact observation 会保留 `visibleArtifactRefs`；`src/runtime/computer-use/package-bridge.ts` 的 action-ledger completion 也会在 artifact intent 缺 current visible artifact 时继续保持未完成；Python loop 的 final attempt 同步 fail closed。Focused 验证：`node --import tsx --test src/runtime/vision-sense/computer-use-plan.test.ts src/runtime/computer-use/package-bridge.test.ts` 36 passed；`PYTHONPATH=packages/actions/computer-use python -m pytest packages/actions/computer-use/tests/test_loop.py -q` 41 passed。
- 进展（2026-05-28）：真实 `CU-NEXT-07` run `/tmp/sciforge-cu-next-real/CU-LONG-004/cu-next-07-real-final-artifact-gate-20260528T1028Z` 已通过 current-round live acceptance 和 completion-grade gate。四轮 trace 均为 real/passed，累计 24 个 GUI actions、23 个 non-wait actions、78 个 screenshot refs、4 个 action ledger 和 4 个 failure diagnostics；round-04 产出 bundle-local `report.md`，`gui.present` artifactRefs 包含 `report.md`，acceptance manifest 为 `multi-app-workflow-passed`，并通过当前 round canonical `isolated-desktop-l3-workflow-evidence.json` 写出 `completionEvidenceRef`。`npm run computer-use-next:validate-run --silent -- --task CU-NEXT-07 --manifest /tmp/sciforge-cu-next-real/CU-LONG-004/cu-next-07-real-final-artifact-gate-20260528T1028Z/manifest.json --json` 返回 `ok=true` / `status=ok`、无 missing refs；readiness 现在把 CU-NEXT-07 标为 evidence-ready，剩余唯一 CU-NEXT-07 blocker 是 PROJECT checklist 状态，本节已勾选。

## 验证规则

- 纯文档改动：运行 `git diff --check`。
- Computer Use package 代码改动：运行 package-local Python suite。
- policy/manifest TypeScript 改动：运行 focused Node tests。
- 可见复杂工作声明必须包含 trace/result refs、screenshot/viewer refs、final artifact refs、verifier verdict、virtual input logs、evidence ledger 和 isolation flags。
- L3 声明还必须证明同一真实 isolated session 中 source -> writer -> file-preview 的跨应用 causality；package-owned fixture、旧 trace、readiness manifest 或 validator skeleton 只能作为 contract/diagnostic。
- L3 partial same-session runtime refs 永远只能作为诊断/辅助输入，不能提升或复制为 completed namespace refs；completed L3 必须由真实 same-session runner 重新产出 top-level completed refs，并被 completed L3 validator 接受，且 `status=completed`、`l3Workflow.completed=true`、`diagnosticOnly=false`、`userAcceptanceEligible=true` 同时成立。
- CU-LONG/CU-NEXT aggregate action quota 只分摊到剩余 action-eligible rounds；refs-only summary/report round 不能吸收 shortfall，但每个 round-scoped completion 仍必须有当前轮 non-wait GUI action evidence。
- artifact/report/summary/index/visual-evidence 类任务必须有当前轮可见 final artifact/report ref 和 `gui.present` / verifier 证据，planner 或 action ledger 不得只凭截图、旧 trace、旧 artifact refs 或 summary 文本提前完成。
- CU-NEXT completion-grade `completionEvidenceRef` 只能是当前 acceptance evidence bundle 顶层的 canonical `isolated-desktop-l3-workflow-evidence.json` regular file；旧 `/tmp` L3 run、跨轮相对路径、绝对路径、symlink、`artifact:` 伪 ref 或 inline shape 都不能完成任务。
- CU-NEXT readiness 必须同时满足真实 task evidence gate 和 PROJECT checklist acknowledgement：`validate-run status=ok` / live acceptance / completion-grade 证明 evidence，PROJECT checkbox 只是 release-board acknowledgement。若 evidence 已通过但 checkbox 未勾，readiness 可标为 `evidence-ready`，但不能计入 passed；checkbox 单独不能替代 evidence。
- native 或 real-app blocked 状态可以接受，但必须写 blocked manifest，说明缺失 capability、安全原因或隔离原因。

## 暂缓集成

以下内容仍未作为默认 release gate 完成；其中 runtime bridge、`gui.present` / `gui.ask_user` metadata、CU-NEXT validation/readiness 已有 refs-first package bridge 与 smoke gate，但还不能替代真实 CU-NEXT run checkbox：

- full SciForge runtime/browser acceptance as a default release gate
- user-visible `gui.present` / `gui.ask_user` receipt UI beyond refs-first metadata
- wiring the now-passed CU-NEXT-01..07 seven-task matrix into the default release gate
- AgentServer/provider registry migration
- release gates and full-repo verification

## 必读文档

- [`packages/actions/computer-use/README.md`](packages/actions/computer-use/README.md)
- [`packages/actions/computer-use/vision_computer_use_agent_mvp.md`](packages/actions/computer-use/vision_computer_use_agent_mvp.md)
- [`packages/actions/computer-use/KV_GROUND_SERVICE_GUIDE.md`](packages/actions/computer-use/KV_GROUND_SERVICE_GUIDE.md)
- [`packages/observe/vision/README.md`](packages/observe/vision/README.md)

## Worktree 规则

- 开发默认在 `dev` 分支；长期分支尽量只保留 `main` 和 `dev`。
- `config.local.json`、`config.computer-use.local.json`、`.sciforge/**`、package caches、runtime homes 等本地状态不得进入 Git。
- 不使用 `git reset --hard` 或 `git checkout --` 擦除用户改动。
- 只清理明确的 generated caches、temporary workspaces 和 build outputs。
