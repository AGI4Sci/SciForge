# SciForge Product Notes

Research Memory should make the agent explain why the next experiment changes.

The expected user-visible story is:

Yesterday the agent ran several iterations. Today it can identify the best current baseline, avoid weak routes, distinguish active evidence from hypotheses, and trace decisions back to run ids, metrics, logs, and artifacts.
