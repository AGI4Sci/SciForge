# Full Collaboration Stage 3 coverage matrix

This matrix is the implementation ledger for OpenSpec change
`add-full-multi-user-collaboration-loop`, tasks 3.4, 3.8 and 3.9. It records the
recovery baseline at `1d47941dabae8185efec4f27a036d1da7908df32`, the accepted
donor behaviour, the Stage 3 implementation, and its executable evidence. The
Cloud database remains the source of truth; WSS is only an Inbox-availability
hint. Stage 3 completion still depends on the final gate ledger at the end of
this document.

## Donor and baseline decision

| Source | Reused in the recovery baseline | Stage 3 decision |
| --- | --- | --- |
| Current commits `515539d7`, `18e4cd37`, `92fd26a0`, `5251d47b`, `de63aa33`, `7eb7fb62`, `e5dc419d`, `77a0ad24`, `5b867e67`, `c92f6026`, `1d47941d` | Worker execution journal/restart and late-result fencing; result/review loop; Coordinator HCI and transfer; independent membership/readiness/authority; provisioning; uncertain-write recovery; content binding; audit-safe recovery marker; content review; Stage 2 regression gates. | Keep as the Stage 3 baseline. Add characterization at the existing canonical service, repository and Desktop store boundaries. |
| B `79200c70`, `f23c6788`, `4a48efa7`, `32be3a74`, `0b1e8043`, `42d1d9ee` | Workbench intent, completion/recovery intent, execution-journal behaviour, durable WSS refill/ACK behaviour. These behaviours are already rewritten into the current Coordinator and `domain-collaboration` owners. | Do not cherry-pick. Reject the old `project-coordinator/worker-runner`, `task.transition`, `resource.create`, `sourceKind`/`targetUserId`, and Collaboration-owned identity/session paths. |
| Old `codex/full-collaboration-loop` | No additional canonical recovery path. | Do not merge: relative to the recovery baseline it removes substantial Stage 2 recovery, Coordinator and Content Space coverage. Use only as comparison evidence. |
| A / C / E donors | A schema lineage and PostgreSQL route fixtures; C identity threat cases; E generic system download/upload-new ownership. | Reconstruct only the supported database fixtures and missing characterization. Do not import old auth, provider, or feature-specific facades. |

## 3.4 offer and execution recovery

| Frozen requirement | Existing implementation and evidence | Baseline gap / Stage 3 evidence |
| --- | --- | --- |
| `task.offer.withdraw` is the sole offer cancellation command; `revoke` is Device/Agent authority revoke. | Strict protocol union exposes `task.offer.withdraw`; contract negative tests reject `task.offer.revoke` and every public client-authored timeout command. | `collaboration-contracts/src/protocol.test.ts`; server public dispatch remains unchanged. |
| Accept/reject/withdraw are exact-offer, exact-execution, revision-checked and idempotent. | `CollaborationService` locks Task/Execution/Offer and uses the common transactional receipt path. Exact replay returns the committed fact; request drift conflicts without revision advance. | `cloud-state.service.test.ts` terminal transition suites and `outbox.test.ts` response-loss replay. |
| Timeout is decided only by Cloud current time and durable `expiresAt`; it is idempotent and creates no successor. | One internal Cloud expiry reconciliation is invoked by the sole startup/scheduler owner. It persists Offer `timed_out`, Execution `timed_out` + `offer_timed_out` fence, Task `revision_requested`, Inbox and internal receipt atomically. Repeat scan is a no-op. | `cloud-state.service.test.ts`: “durably times out an expired offer once…”; `bootstrap.test.ts` startup reconciliation. |
| Only the current Coordinator may reassign after reject/timeout/withdraw/revoke. | Reassign requires the current Coordinator actor, Project authority epoch, exact predecessor revisions and one admitted retryable terminal fence. Pending/accepted/running and unrelated terminal facts are rejected. | `cloud-state.service.test.ts`: “rejects Coordinator reassignment until…” and transfer/reassign suite. |
| Every reassignment creates a fresh `executionId`; an old execution is an immutable audit fact and permanently fenced. | Reject, timeout, withdraw, Agent revoke, Device revoke and review revision all retain the exact predecessor and create a distinct successor only after the explicit Coordinator write. | Stage 3 recovery block in `cloud-state.service.test.ts`; real restart comparison in `stage3-postgres.integration.test.ts`. |
| Every late old-execution write is rejected. | `assertOpenCurrentExecution` gates offer decisions, start/fail/preflight, HumanNeeded, result, resource/file association, review and recovery. Tests retain an exact pre-write snapshot and assert no mutation after denial. | Device revoke late-write suite plus timeout/withdraw/reassign late writes in `cloud-state.service.test.ts`. |
| Device/Agent revoke, logout/ownership conflict or Principal lease loss stops command/WSS, Runtime and file access and fences current execution. | Device revoke now atomically revokes its Agents/credentials/availability and fences their current executions. Identity fences HTTP/WSS synchronously; Collaboration aborts Runtime and local file work before reconnect. | `identity-service.test.ts`, `websocket.test.ts`, Identity Agent runtime tests, Collaboration `connection.test.ts` and `task-adapter.test.ts`. |
| Coordinator transfer targets an active Agent owned by the same Project Owner and immediately fences old Coordinator-only writes. | Owner-only transfer checks target ownership/active availability, bumps Project revision and authority epoch, sends durable Inbox feedback and rejects the old Coordinator. | Server transfer suite and `project-coordinator/src/coordinator-transfer.test.ts`. |
| Provider removal is independent of metadata visibility; uncertain writes remain `outcome_unknown` until exact observe/link or abandon. | Existing service, Content Space/OpenContent, Worker and Coordinator recovery paths prove isolated unauthorized state, exact observation/link or abandon, no automatic Provider retry and never-used successor names. | Server recovery suites, Collaboration `task-adapter.test.ts`, Coordinator `recovery.test.ts`, Content Space/OpenContent focused tests. |

## 3.8 PostgreSQL durability

| Requirement | Existing implementation and evidence | Baseline gap / Stage 3 evidence |
| --- | --- | --- |
| Supported routes are exactly `fresh-v4`, `upstream-v4`, `public-v5`, `staging-v9`, `a-v11`, `current-v12`, `current-v13`, `current-v14`. | `migrations.ts` admits only frozen source fingerprints and validates the converged v14 lineage fingerprint. Historical A 0001–0009 fixtures are byte-identical donor evidence. | Real PostgreSQL test builds and migrates all eight routes and rejects unknown drift before forward mutation. |
| Fresh install, repeated startup/no-op, forward-only route and final catalog fingerprint. | Every formal source and final catalog is compared to its frozen fingerprint; a second migration is a no-op. A `public-v5 → v14` is exercised on PostgreSQL 17. | `stage3-postgres.integration.test.ts`: all-route migration case. |
| Migration failure rolls back. | Migration holds one acquired connection and explicitly rolls back before release. An injected v12 legacy-binding failure retains the exact version/catalog/data snapshot, and the connection remains reusable. | `stage3-postgres.integration.test.ts`: failure rollback case. |
| Task/Execution/Offer/fence, Inbox and business idempotency receipt commit atomically. | A real repository failure is injected at `insertReceipt`, after business rows and Inbox were attempted. Task/Execution/Offer/Inbox/receipt are all absent after rollback; the later normal command commits all together. | `stage3-postgres.integration.test.ts`: transactional commit case. |
| Cloud restart preserves revision, execution, Inbox, receipt, idempotency and external-operation journal. | Pool/repository/service are reconstructed on the same database. Exact revisions/fence, Inbox sequence/cursor, business and ACK receipt responses, idempotency replays and `outcome_unknown` journal remain identical with no duplicate rows. | `stage3-postgres.integration.test.ts`: restart reconstruction case, including the same Coordinator ACK before and after Server reconstruction. |
| No Cloud/Desktop distributed transaction. | Desktop store separately commits processed message/sequence/ACK intent and Runtime Session/directive binding; Provider writes separately use durable prepare/dispatch/observation. | Collaboration `connection.test.ts`, `task-adapter.test.ts`, AgentRuntime directive-ledger test, and the Stage 3 recovery runbook. |

## 3.9 public recovery and security surface

All public JSON schemas are strict. Write commands use a body idempotency key
that must equal the `Idempotency-Key` header. The default REST body limit is 64
KiB and the configured value is clamped to 1 MiB; WSS frames are limited to 8
KiB and compression is disabled. Collection/page schemas impose their own
bounds in addition to the transport bounds.
Rows group only commands that share the same authorization and recovery owner;
every recovery-loop command in a grouped row is named explicitly.

| Surface | Actor / audience and resource ownership | Revision / epoch / idempotency | Bounds and safe failure | Required matrix evidence |
| --- | --- | --- | --- | --- |
| `GET /v1/me`, `GET /v1/me/devices` | OIDC User; only the authenticated User and their Devices. | Read-only; current OIDC/local identity validation at the Identity boundary. | No request body; strict bounded response; no credentials returned. | `identity-api.test.ts`: positive, missing/Agent actor, foreign isolation and redaction; `identity-service.test.ts`: inactive identity. |
| `POST /v1/device-enrollments`, `POST /v1/devices` | OIDC User; enrollment and Device ownership are bound to that User. | Business idempotency receipt; enrollment expiry and single-use proof; Device key attestation. | REST body limit plus strict key/attestation schemas. | Positive, wrong owner/expired enrollment, mismatched header, duplicate request. |
| `DELETE /v1/me/devices/:deviceId` | OIDC User; only a Device owned by that User. | Expected ownership fact and idempotency receipt; path/body ID equality. | REST body limit; opaque not-found/denial; no credential material. | Positive, foreign Device, stale/revoked Device, mismatched path, duplicate revoke. |
| `endpoint.challenge.create` / `endpoint.challenge.get`, `endpoint.transition` / `endpoint.transfer` | OIDC User; challenge and endpoint are exact User/provider-realm facts, and only the endpoint owner may mutate or transfer them. | Challenge create is receipt-idempotent before its durable per-User/provider/realm rate window; one-time code is never reissued. Endpoint writes require exact revision and idempotency. | Challenge fields are bounded; five unique creates per five-minute window, then safe `rate_limited`; duplicate one-time material returns safe conflict. | `api.test.ts`, `service.test.ts`, endpoint ownership/transfer tests: positive, foreign owner, stale revision, exact replay, sixth request, safe error and no polling credential. |
| `agent.register`, `agent.rotate_credential`, `agent.revoke`; `agent.heartbeat` | Owning OIDC User registers/rotates/revokes; exact Device Agent heartbeats only itself. | Agent/Device revision, credential generation/authority epoch and business idempotency. | Capability arrays max 256; strict one-time sealed credential envelope; duplicate one-time material is not reissued. | Identity Agent runtime and server authority suites: positive, cross-Owner/Agent, stale revision/epoch, duplicate write, revoked Device and no credential reflection. |
| `provider_directory_principal.publish` / `.list`, `worker.availability.publish` / `.list` | Each OIDC User publishes only their own Provider principal fact; each Agent publishes only its own availability. Project-scoped reads require active membership. | Device/Agent and fact/availability revisions, observation time and idempotency; no metadata field grants Provider ACL. | Pages and capability tags are bounded; responses contain factual references, never Provider credentials. | Cloud state Provider/availability suites: positive, foreign publisher/project, stale Device/Agent/fact revision, duplicate and unauthorized Provider observation. |
| `project.create` / `.list` / `.get` / `.coordination.read` / `.transition` | OIDC User creates and reads only owned/member Projects; only Owner may administer lifecycle. The coordination read is the sole paged Cloud workspace reconstruction. | Project revision, execution authority epoch and idempotency on writes; reads return current independent pages. | Project budgets/text/pages are bounded; safe denial does not reveal foreign Project state. | Cloud workspace and state tests: create/read, foreign/non-member denial, stale transition revision/epoch, duplicate create/transition and paged restart reconstruction. |
| `project.membership.add` / `.remove` / `.list`, `project.task_authority.list` | Project Owner mutates Membership; active members read. Membership, Content Readiness, Provider Observation and Task Authority remain independent. | Project/membership/readiness/binding revisions, authority epochs and idempotency. | Bounded members/pages; no Provider ACL fallback and no credential/locator authority leakage. | Dynamic membership and Provider-removal suites: positive, non-Owner, stale facts, duplicate add/remove, removed member fence and unaffected sibling member. |
| `project.content.provisioning_intent.get`, `project.content.attest`, `project.content.observation.submit`, `project.content.binding.get` / `.close` | Project Owner OIDC User submits signed factual provisioning/observation; active Project actors may read exact intent/binding. | Exact Project/intent/binding/member/fact revisions, Device signature fact and idempotency. | Strict bounded portable references and digests; signing payload has no Token or Provider credential. | Server provisioning/recovery suites and Coordinator `provisioning.test.ts`: positive, foreign actor, stale tuple/signature, duplicate, root loss and member-only unauthorized. |
| `external_operation.prepare` / `.dispatch` / `.observe`, `project.content.recovery.abandon` | The exact authorized Human/Agent operation owner advances one journal; only Project Owner abandons Project-content recovery. | Journal + Project/intent or Task/Execution revisions and idempotency; prepare → dispatch → observation is monotonic. | Operation/request digests and safe errors are bounded; `outcome_unknown` forbids automatic retry. | Server external-operation suites and Coordinator provisioning recovery: exact replay, cross-owner/stale tuple denial, uncertain observation, abandon and no second Provider write. |
| `project.plan.submit` / `.confirm`, `task.offer.create` | Current Coordinator Agent submits immutable Plan and creates offers; Project Owner confirms; assignee must be one exact eligible Agent. | Project/Plan/Coordinator epoch/availability revisions and idempotency. | Plan/task/file intents and counts are bounded; no caller-supplied Cloud authority or credential. | Server Plan/offer suites and Coordinator `plan-workflow.test.ts`: positive, Human/old Coordinator/foreign Agent denial, stale revisions, duplicate submit/confirm/create. |
| `task.offer.accept` / `task.offer.reject` | Exact offered Worker Agent on the assigned Device. | Offer + Task + Execution revisions and business idempotency. | Strict reason/text bounds; Cloud `expiresAt` check. | Positive, wrong Agent/Device, stale revision, duplicate, expired offer. |
| `task.offer.withdraw` / `task.offer.reassign` | Current Coordinator Agent only; target is an eligible exact active Agent in the same Project. | Project Coordinator authority epoch, Task/Offer/Execution revisions and idempotency. | Strict IDs/reason/file intent; no `revoke` alias or timeout command. | Positive, old/foreign Coordinator, illegal predecessor, stale epoch/revision, duplicate. |
| `task.execution.start` / `task.execution.fail` / `task.execution.preflight.get` | Exact current Worker Agent/Device/execution. | Execution + Task revision and all persisted authority epochs; writes idempotent. | Strict timestamp/error fields; preflight exposes bounded reason facts, not secrets. | Positive, wrong actor, old fenced execution, stale revision/epoch, duplicate writes. |
| `human.needed.create`, `task.result.submit` | Exact current Worker execution. | Execution/Task/authority revisions and idempotency; execution-bound file references. | Bounded text/outputs; strict portable references; safe Provider failures. | Positive, cross-execution, stale fence, duplicate and removed Provider. |
| `task.result.review` | Current Coordinator Agent only; Owner Human may approve/answer but cannot write the formal review. | Coordinator authority epoch, Task/Execution/result revisions and idempotency. | Bounded decision/instruction; strict next file intent. | Server review and completion suites; HTTP Human bypass denial in `api.test.ts`. |
| `task.recovery.link_observed_output`, `task.recovery.abandon` | Project Owner OIDC User acts through Coordinator HCI on the exact recovery tuple; only the current Coordinator Agent may create the successor execution. | Coordinator authority epoch plus Task/Execution/recovery action/journal revisions and idempotency. | Exact observed output identity; no mark-success; successor enforces a never-used safe output name. | Server outcome-unknown suites, Coordinator `recovery.test.ts`, Worker recovery Inbox suites. |
| `project.transfer_coordinator` | Project Owner User; target must be another active Owner-owned Agent. | Project revision + Coordinator authority epoch + idempotency; transfer bumps the epoch. | Strict IDs; no target credential/route input. | Positive, non-Owner/foreign Agent, stale epoch/revision, duplicate and old-Coordinator fence. |
| `human.answer`, `project.decision.submit`, `project.final_summary.submit` | Target Owner User alone answers; current Coordinator Agent alone converts the answer into a ProjectRecord/decision and completes the Project. | Human request/answer, Project, result and Coordinator epoch revisions plus idempotency. | Bounded prompt/answer/summary; confirmation facts are explicit and errors are safe. | `identity-api.test.ts` HumanNeeded approval and Coordinator `coordinator-actions.test.ts`: wrong Human/Worker/old Coordinator, stale revisions, duplicate Inbox and atomic completion. |
| `project.input.create`, `project.endpoint.bind` / `.update` / `.get` | Verified Human endpoint creates exact provider input; Project Owner manages one endpoint binding; active members read it. | Provider message identity, binding/Project revisions and idempotency. | Strict bounded text/portable locator; public OIDC route cannot impersonate provider gateway. | API provider-gateway denial, server input/binding ownership and duplicate-provider-message tests, Coordinator durable Inbox decision test. |
| `task.get`, `resource.get`, `receipt.get` | Active Project actor reads only a Task/resource reachable through membership; a receipt is readable only by its exact actor. | Read-only current facts; receipt binds actor key, request digest, operation and idempotency key. | Strict IDs; resource envelope and receipt response are bounded and credential-free. | Server ownership/read tests: positive, cross-Project/actor denial, fenced current revision and receipt replay after restart. |
| `inbox.pull` / `inbox.ack` | User pulls own User Inbox; Agent pulls exact own Agent Inbox. | Durable monotonic sequence; ACK write is idempotent and recipient-bound. | `afterSequence` bounded to safe integer; page 1..1000 contract and service clamp 1..200; response max 1000. | Positive, cross-recipient, duplicate page/message/ACK, stale/out-of-order ACK, reconnect refill. |
| `GET WSS /v1/events` | Authenticated OIDC User or exact Agent Device; audience is only that principal's Inbox. | Authentication/lifecycle checked at connect; no business writes; sequence refill remains REST/SDK-owned. | 8 KiB frames, no compression, origin allowlist for supplied browser origins, strict ping/pong/error envelopes. | `websocket.test.ts`: bad origin/auth, exact audience, no business payload, strict frames, oversize and revoke close; `connection.test.ts`: refill. |
| Identity-owned token-free SDK transports | Domain callers receive allowlisted request/response contracts only; Identity privately injects OIDC or Agent credentials. | Lifecycle generation/authority epoch is checked before and after I/O; idempotency forwarded exactly. | 1 MiB authenticated response and 2 MiB Agent response limits; queue max 1000; URLs/headers are not caller-controlled. | Identity transport/runtime tests: allowlist, secret alias rejection, bounds, in-flight fence, pre/post lifecycle and WSS close. |
| AgentExecution Host Session/turn | Domain caller receives only bounded `runtimeId/threadId/turnId`; no Provider or model credential. Worker persists Session and directive before dispatch. | Stable `runtimeId/threadId/clientDirectiveId` reconciles one Host ledger fact across restart. | Strict session/request/result schemas; bounded prompt, IDs, metadata and tools. | SDK contract test; production domain Host test; AgentRuntime “does not dispatch…twice”; Worker restart characterization. |

## Executable evidence index

| Boundary | Positive / ownership | stale revision or authority | duplicate / restart | bounds / redaction |
| --- | --- | --- | --- | --- |
| REST Identity | `identity-api.test.ts` owner enrollment/create/list/revoke | foreign Device, stale recent-auth, inactive identity | one Device fact; revoke replay survives stale auth; enrollment nonce is not reissued | 1 KiB configured limit, header/path mismatch, no Token/signature/nonce reflection |
| REST recovery commands | `api.test.ts`, `cloud-state.service.test.ts` | every Task/Execution/Project CAS and Coordinator epoch denial | common receipt replay, monotonic ACK and Cloud reconstruction | strict contract schemas, durable challenge rate bound and safe `rest.error` correlation |
| SDK User/Agent | Identity authenticated transport and Agent runtime tests | lifecycle generation and Device/Agent fence before and after I/O | exact idempotency/request forwarding and stable directive ledger | response/queue limits, command allowlists, recursive secret-alias rejection |
| WSS | `websocket.test.ts` ready/ping/audience/revoke | bad bearer/origin and authority disconnect | only hint; Desktop REST drain owns replay/ACK | 8 KiB, text-only, strict message union, safe close codes |
| Desktop Inbox | Collaboration `connection.test.ts` | authority loss fences before reconnect | duplicate page/message and reconnect produce one handler fact/ACK; multi-page drain | page 100, durable sequence, no socket business payload |
| Runtime / Provider | Worker `task-adapter.test.ts`, Coordinator `recovery.test.ts` | execution/membership/Device fence and exact recovery tuple | one Runtime provider turn after restart; no upload retry from `outcome_unknown` | stable safe IDs/digests; no credentials or absolute paths in public recovery command |

## Completion ledger

- [ ] 3.4: characterization, implementation and focused recovery gates complete.
- [ ] 3.8: every route and rollback/transaction/restart case proven on real PostgreSQL.
- [ ] 3.9: public matrix tests, security audit and recovery operations runbook complete.
- [ ] Stage 3 gates recorded with exact command counts, skips and failures.
- [ ] OpenSpec 3.4/3.8/3.9 updated only after the matching evidence above passes.

Stage 4 packaged, multi-device and live recovery evidence remains explicitly out
of scope here; OpenSpec 8.3, 8.4 and 8.7 remain unchecked.
