{
  "name": "@agi4sci/bioagent-web-ui",
  "version": "0.1.0",
  "description": "BioAgent React web UI and design documentation.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "test": "node --import tsx --test \"ui/src/**/*.test.ts\"",
    "verify": "npm run typecheck && npm run test && npm run smoke:all && npm run build",
    "smoke:all": "npm run smoke:fixtures && npm run smoke:skill-registry && npm run smoke:seed-runtime && npm run smoke:knowledge-unsupported && npm run smoke:repair && npm run smoke:agentserver-generation && npm run smoke:agentserver-repair && npm run smoke:workspace-agentserver-repair && npm run smoke:view && npm run smoke:omics-runners",
    "smoke:fixtures": "tsx scripts/smoke-bioagent-fixtures.ts",
    "smoke:repair": "tsx scripts/smoke-repair-needed.ts",
    "smoke:agentserver-generation": "tsx scripts/smoke-agentserver-generation.ts",
    "smoke:agentserver-repair": "tsx scripts/smoke-agentserver-repair.ts",
    "smoke:workspace-agentserver-repair": "tsx scripts/smoke-workspace-server-agentserver-repair.ts",
    "smoke:skill-registry": "tsx scripts/smoke-skill-registry.ts",
    "smoke:seed-runtime": "tsx scripts/smoke-seed-runtime.ts",
    "smoke:knowledge-unsupported": "tsx scripts/smoke-knowledge-unsupported.ts",
    "smoke:view": "tsx scripts/smoke-view-composition.ts",
    "smoke:omics-runners": "tsx scripts/smoke-omics-runners.ts",
    "workspace:server": "tsx scripts/workspace-server.ts",
    "preview": "vite preview --host 0.0.0.0",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "3dmol": "^2.5.4",
    "d3": "^7.9.0",
    "lucide-react": "^1.8.0",
    "react": "^19.2.5",
    "react-dom": "^19.2.5",
    "recharts": "^3.8.1"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.1",
    "tsx": "^4.21.0",
    "typescript": "^5.7.0",
    "vite": "^8.0.8"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
