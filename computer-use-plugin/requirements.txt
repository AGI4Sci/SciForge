# Computer-Use plugin — native GUI-Owl-1.5 driver (no Agent-S).
# Model HTTP client (talks to the GUI-Owl vLLM OpenAI-compatible endpoint).
requests>=2.31.0
# Executor: local desktop control — cross-platform.
pyautogui>=0.9.54
mss>=9.0.0
pyperclip>=1.8.2
Pillow>=10.0.0
